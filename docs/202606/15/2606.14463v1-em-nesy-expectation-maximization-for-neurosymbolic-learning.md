---
title: "EM-NeSy: Expectation Maximization for Neurosymbolic Learning"
title_zh: EM-NeSy：神经符号学习的期望最大化
authors: "Annegret Seibt, Luc De Raedt, Giuseppe Marra"
date: 2026-06-12
pdf: "https://arxiv.org/pdf/2606.14463v1"
tags: ["query:sr-piml"]
score: 8.0
evidence: 基于EM算法的神经符号学习方法，增强可解释性和符号推理
tldr: 现有神经符号学习要求符号组件可微，限制了近似推理的应用。本文提出EM-NeSy，将概率神经符号学习视为期望最大化（EM）算法实例，在期望步通过概率推理计算符号后验，最大化步仅通过神经组件梯度下降更新参数。该方法自然支持近似推理，无需符号组件可微，且在精确推理下恢复标准端到端梯度方法。实验表明具有良好可扩展性和计算效率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.14463v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1729, \"height\": 602, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.14463v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1439, \"height\": 346, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.14463v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1093, \"height\": 349, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.14463v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1215, \"height\": 311, \"label\": \"Table\"}]"
motivation: 现有神经符号模型要求符号组件可微，限制了近似推理的使用。
method: 将概率神经符号学习视为EM算法，E步概率推理后验，M步神经梯度更新。
result: 实验表明方法可扩展且计算高效，在多个任务上取得良好性能。
conclusion: EM-NeSy解锁EM潜力，自然支持近似推理，无需符号组件可微。
---

## 摘要
神经符号（NeSy）模型融合了神经网络与符号推理，以实现稳健且可解释的人工智能。最先进的NeSy模型要求符号组件以可微分方式表达，这往往使近似推理的使用变得复杂。我们提出EM-NeSy，它将概率性NeSy学习视为期望最大化（EM）算法的一个实例。在期望步骤中，我们通过概率推理计算关于标签条件下神经预测符号的后验概率。在最大化步骤中，我们仅通过神经组件使用梯度下降，基于该后验更新神经参数。该公式释放了EM算法在NeSy学习中的全部潜力。它使NeSy能够自然地扩展到近似推理，而无需对符号组件进行任何额外修改或可微性要求。此外，在精确推理下，它恢复为标准端到端基于梯度的NeSy设置。我们的实验结果表明了EM-NeSy的可扩展性和计算效率。

## Abstract
Neurosymbolic (NeSy) models integrate neural networks and symbolic reasoning for robust and interpretable AI. State-of-the-art NeSy models require that the symbolic component is expressed in a differentiable way, often complicating the use of approximate inference. We propose EM-NeSy which casts probabilistic NeSy learning as an instance of the Expectation-Maximization (EM) algorithm. In the expectation step, we compute the posterior over the neurally predicted symbols conditioned on the label via probabilistic inference. In the maximization step, we update the neural parameters based on this posterior using gradient descent only through the neural component. This formulation unlocks the full potential of the EM algorithm for NeSy learning. It allows NeSy to extend naturally to approximate reasoning without any additional modifications or differentiability requirements of the symbolic component. Furthermore, it recovers the standard end-to-end gradient-based NeSy setting under exact inference. Our experimental results demonstrate the scalability and computational efficiency of EM-NeSy.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
神经符号（NeSy）模型旨在融合神经网络的感知能力和符号逻辑的推理能力，以实现稳健且可解释的人工智能。当前主流的 NeSy 方法要求符号组件以可微分的方式表达，以便进行端到端的梯度反向传播。然而，这限制了对近似推理（如采样、迭代推理算法）的使用，因为许多高效的非精确推理方法本身不可微或难以高效求导。这一问题阻碍了 NeSy 在大规模或复杂推理场景下的扩展。论文提出 EM-NeSy，将概率 NeSy 学习重新视为期望最大化（EM）算法的实例，从而在训练过程中将符号推理与神经参数更新解耦，消除了对符号组件可微性的依赖，自然支持各种精确或近似推理引擎。

### 2. 论文提出的方法论：核心思想、关键技术细节

#### 核心思想
将 NeSy 模型视作一个条件潜在变量模型：神经组件输出潜在符号变量 Z 的先验分布 p_Ne(z|x,θ)，符号组件根据背景知识计算标签 Y 的条件分布 p_Sy(y|z)。EM-NeSy 利用 EM 算法交替进行 E 步和 M 步训练：
- **E步**：使用当前神经参数 θ(t)，基于观测样本 (x,y) 计算潜在变量 Z 的后验分布 p(z|y,x,θ) = p_Ne(z|x,θ) p_Sy(y|z) / p(y|x,θ)。此步可由任何推理引擎（精确或近似，可微或不可微）完成，称为“符号校正”（abduction）。
- **M步**：固定计算得到的后验标签 ℓ(z)（可以是软概率或硬 MAP 结果），定义损失函数 L_EM = -∑ ℓ(z) log p_Ne(z|x,θ)，并仅通过神经组件进行梯度下降更新参数 θ。无需梯度通过符号组件。

#### 关键特点
- **等价性**：论文证明（Theorem 3.1）在精确推理下，EM-NeSy 的梯度与标准端到端 NeSy 的梯度严格相等，因此恢复经典设置。
- **灵活性**：E 步可任意选用 exact/approximate 推理（如信念传播、ABC 采样、吉布斯采样等），无需额外修改。
- **EM 变体**：支持广义 EM、Hard EM（MAP 后验）、Online EM（单样本/小批量）、Multi-M EM（多次 M 步后再重计算 E 步），以加速训练或提升稳定性。

### 3. 实验设计：数据集、benchmark、对比方法
- **数据集**：三个经典 NeSy 弱监督基准：
  - **Multi-Digit MNISTAdd**：2n 张 MNIST 图像构成两个 n 位数加法，标签仅提供和。数字个数 n=4,15,100。
  - **Visual Sudoku**：4x4 和 9x9 格子，每格一张 MNIST 图像，标签为二值“是否为有效数独”。
  - **Warcraft Path-Planning**：12x12 和 30x30 网格地图，标签为从左上到右下的最短路径二进制掩码。
- **对比方法**：
  - 端到端可微方法：DeepStochLog (DSL)、Semantic Loss (SL)、Semantic Probabilistic Layer (SPL)、Scallop（top-k 近似）。
  - 采样/近似方法：A-NeSI、I-MLE、EXAL。
  - 本文自身变种：BP-EM（sum-product 信念传播）、BP-EM-M（max-product 硬 EM）、ABC-EM（ABC 采样）。
  - 基线（BP-std）：与 EM 使用相同信念传播推理但端到端反向传播。
- **评价指标**：准确率（%）和训练时间（分钟）。

### 4. 资源与算力
实验使用单张 NVIDIA L40S GPU（48GB 显存），CPU 为 128 线程，256GB RAM。论文未详细说明训练总时长或 GPU 数量，仅在各结果表中记录了训练时间（分钟）。例如，MNISTAdd 4 位数 BP-EM 训练仅 1.33 分钟，100 位数 8.51 分钟；Visual Sudoku 4x4 ABC-EM 1.40 分钟；Warcraft 30x30 ABC-EM 8.37 分钟。这表明所需算力在单卡下可接受。

### 5. 实验数量与充分性
- 实验覆盖了三个不同领域（加法、数独、路径规划）和多种规模（小、中、大）。
- 每个设置报告 5 次随机种子的均值与标准差。
- 对比了多种现有方法，且对每个基准都尽量包含了典型基线（但部分基线因可扩展性问题未能在所有规模上运行）。
- 消融方面：对比了精确推理（BP-EM vs BP-std）和近似推理（ABC-EM、Hard EM），验证了 EM 框架的通用性。
- 充分性判断：实验相对充分，但缺少更多消融研究（如不同推理方法对不同规模的详细性能分解、对近似推理质量的分析）。没有明显的公平性问题（如重复实验、命名规范），但部分基线结果引自原论文（可能在不同硬件环境），存在一定偏差风险。

### 6. 论文的主要结论与发现
- 在精确推理下（MNISTAdd 4/15 位数），EM-NeSy 取得了与标准端到端 NeSy 几乎一致的准确率，且训练更快、显存更低（100 位数时，BP-EM 显存 35.52GB vs BP-std 90.61GB）。
- 在近似推理下，EM-NeSy 能够无缝集成非可微推理算法（如 ABC 采样、Max-Product BP），扩展至标准端到端方法超时或不可行的规模（如 15 位数以上加法、9x9 数独）。
- 近似推理的质量对最终性能影响显著：ABC-EM 在小规模上达到最优，但在大规模上方差较大；Hard EM 易陷入局部最优；Loopy BP 在 9x9 数独上因图高度有环而失败。
- 总体表明 EM-NeSy 提供了一个统一、灵活且可扩展的 NeSy 学习框架，但性能依赖于推理引擎的近似精度。

### 7. 优点
- **方法创新**：首次将 EM 算法系统性地引入 NeSy 学习，理论完备（证明与端到端梯度等价），且清晰揭示了 NeSy 与概率潜在变量模型的联系。
- **解耦推理与学习**：E 步可使用任何黑箱推理引擎，无需可微性要求，极大拓宽了 NeSy 的适用范围（例如可集成成熟的贝叶斯推理工具）。
- **效率提升**：避免反向传播通过推理迭代，大幅降低训练时间和峰值显存，且自然而然地支持 Mini-batch 和在线学习。
- **实验验证全面**：覆盖多任务、多规模，对比多种现有方法，展示了准确率与效率的权衡。

### 8. 不足与局限
- **未解决近似推理选择问题**：论文仅指出可以灵活使用各种推理引擎，但未提供如何针对具体问题自动选择最优近似方法的指导，用户需手动调参。
- **实验覆盖有限**：缺少对其他近似推理方法（如变分推理、MCMC）的实验；也缺少对 Multi-M EM 等变体的深入分析（表格中 Multi-M 性能较差且方差大，未充分解释原因）。
- **可重复性风险**：部分基线结果（如 A-NeSI、EXAL）引自原论文，硬件不一致可能导致比较略有偏差。
- **应用限制**：当后验近似质量极差时（如 BP 在高度有环图上、ABC 在大搜索空间中采样效率低），训练效果可能崩溃，论文指出了这一点但未给出稳定解决方案。
- **缺乏对连续潜在变量的讨论**：论文虽提到理论上可扩展到连续变量，但实验全部基于离散符号，未验证连续性场景。

（完）
