---
title: "Dual-Network PINNs for Optimal Control: A Reproducible Benchmark on the Mass-Spring-Damper System"
title_zh: 双网络PINNs用于最优控制：质量-弹簧-阻尼系统的可复现基准测试
authors: "Abdeladhim Tahimi, Rinaldo Vieira da Silva Junior"
date: 2026-06-13
pdf: "https://arxiv.org/pdf/2606.15271v1"
tags: ["query:sr-piml"]
score: 9.0
evidence: 双网络物理信息神经网络用于质量-弹簧-阻尼器系统最优控制
tldr: 针对质量-弹簧-阻尼器系统的最优控制问题，本文提出了一种直接双网络物理信息神经网络（PINN）方法。该方法使用两个前馈神经网络分别近似状态和控制，通过复合三次掩膜精确施加状态边界条件，损失函数结合物理残差和成本函数的梯形近似。在基准测试中，PINN以四位有效数字重现经典最优成本，点态状态和控制误差与经典方法相当，且精确满足终端约束。尽管训练时间比经典shooting慢两个数量级，但本研究提供了清晰可复现的实现，显著降低了从业者探索PINN最优控制的门槛。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.15271v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1348, \"height\": 744, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.15271v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1347, \"height\": 728, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.15271v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1345, \"height\": 722, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.15271v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1350, \"height\": 728, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.15271v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1347, \"height\": 727, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.15271v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1348, \"height\": 729, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.15271v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 944, \"height\": 549, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.15271v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1107, \"height\": 257, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.15271v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1112, \"height\": 591, \"label\": \"Table\"}]"
motivation: 提供透明可复现的PINN最优控制基准，降低无伴随方法经验从业者的入门门槛。
method: 双网络PINN：状态网络用复合三次掩膜精确满足边界，控制网络无约束；损失函数融合物理残差与成本函数梯形近似。
result: PINN以四位有效数字重现经典最优成本，精确满足终端约束，点态误差与经典方法相当，训练慢两个数量级。
conclusion: 双网络PINN能有效求解最优控制问题，且实现可复现，适合作为入门基准。
---

## 摘要
本工作提出了一项透明且可复现的基准研究，采用直接双网络物理信息神经网络（PINN）方法来解决质量-弹簧-阻尼系统的最优控制问题。经典的线性二次型最优控制问题通过两种独立经典方法求解——庞特里亚金最小值原理配合单次打靶法，以及通过梯形配点法直接转录——并重新表述为一个约束优化问题，由两个前馈神经网络求解：一个状态网络通过复合三次掩膜ansatz精确施加边界条件，以及一个无约束控制网络。复合损失结合了配点处的物理残差和成本泛函的梯形近似，并由单个标量超参数加权。在所考虑的基准测试中，PINN将经典最优成本再现到四位有效数字，精确满足终端状态约束，并产生点态状态和控制误差，这些误差落在两种经典参考结果的散布范围内。在该基准测试中，训练速度比经典打靶法慢约两个数量级，已如实报告。贡献在于方法论的清晰性而非新颖性：该公式及附带的Google Colab实现旨在降低实践者探索基于PINN的最优控制的入门门槛，而无需事先了解伴随方法或两点边值问题。

## Abstract
This work presents a transparent and reproducible benchmark study of a direct dual-network Physics-Informed Neural Network (PINN) formulation for the optimal control of a mass-spring-damper system. The classical linear-quadratic optimal control problem is solved by two independent classical methods -- Pontryagin's Minimum Principle with single shooting, and direct transcription through trapezoidal collocation -- and recast as a constrained optimization problem solved by two feedforward neural networks: a state network whose boundary conditions are enforced exactly through a composite cubic-and-mask ansatz, and an unconstrained control network. The composite loss combines the physics residual at the collocation points with a trapezoidal approximation of the cost functional, weighted by a single scalar hyperparameter. On the benchmark considered, the PINN reproduces the classical optimal cost to four significant digits, satisfies the terminal state constraints exactly by construction, and produces pointwise state and control errors that fall within the spread of the two classical references. Training is approximately two orders of magnitude slower than classical shooting on this benchmark, which is honestly reported. The contribution is methodological clarity rather than methodological novelty: the formulation and the accompanying Google Colab implementation are intended to lower the barrier to entry for practitioners exploring PINN-based optimal control without prior exposure to adjoint methods or two-point boundary value problems.

---

## 论文详细总结（自动生成）

# 双网络PINNs用于最优控制：质量-弹簧-阻尼系统的可复现基准测试——详细总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：如何在最优控制问题中，采用物理信息神经网络（PINN）以简洁、可复现的方式逼近经典解，同时降低从业者的入门门槛。
- **背景与动机**：
  - 经典最优控制方法（庞特里亚金最小值原理、直接转录法）数学成熟、精度高，但在非线性、高维、多物理场耦合或逆向问题中实现困难，推导伴随系统与两点边值问题对新手存在显著概念和计算障碍。
  - PINN近年来被广泛用于求解微分方程，并逐渐向最优控制扩展，可统一处理状态近似、控制优化、参数估计与物理约束。
  - 现有文献日益复杂，缺乏面向初学者的透明基准，特别是将经典最优控制公式与直接PINN优化策略进行对比、提供可复现代码的工作较少。
- **本研究目标**：不以提出新颖架构或宣称超越经典为宗旨，而是提供教学导向的透明基准，说明如何将约束最优控制问题转换为直接PINN框架，并通过质量-弹簧-阻尼器系统验证其可复现性与精度。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **问题设定**：
  - 系统方程：\( m\ddot{x} + c\dot{x} + kx = u(t) \)，取\( m=k=c=1 \)（阻尼比0.5，欠阻尼）。
  - 性能指标：\( J[u] = \int_0^T [x(t)^2 + r u(t)^2] dt \)，\( r=0.1 \)，\( T=5 \) s。
  - 初始条件\( x(0)=1, \dot{x}(0)=0 \)，终端约束\( x(T)=0, \dot{x}(T)=0 \)。
- **经典解法（两条独立参考线）**：
  - **PMP+单次打靶法**：引入协态变量，推导两点边值问题，通过数值积分+求根确定初始协态。
  - **直接转录（梯形配点法）**：在N=100均匀网格上将问题离散为NLP，用梯度优化器求解。
- **PINN方法（核心贡献）**：
  - **双网络架构**：
    - 状态网络\( \mathcal{N}_x(t;\theta_x) \)输出原始值，经过复合ansatz获最终近似\( \hat{x}(t) \)。
    - 控制网络\( \mathcal{N}_u(t;\theta_u) \)直接输出\( \hat{u}(t) \)，无约束。
  - **硬边界条件强制**：
    - 立方多项式\( \phi(t) = 1 - 3(t/T)^2 + 2(t/T)^3 \)满足四个边界条件。
    - 掩膜函数\( \psi(t) = t^2(T-t)^2/T^4 \)在端点处为零且一阶导为零。
    - 复合ansatz：\( \hat{x}(t) = \phi(t) + \psi(t) \cdot F_x(t) \)，保证边界条件恒成立。
  - **损失函数**：
    - 物理残差\( \mathcal{L}_{\text{phys}} = \frac{1}{N_c}\sum_i (\ddot{\hat{x}}_i + \dot{\hat{x}}_i + \hat{x}_i - \hat{u}_i)^2 \)。
    - 成本项\( \mathcal{L}_{\text{cost}} \)为梯形法则近似积分。
    - 总损失\( \mathcal{L} = \lambda_{\text{phys}}\mathcal{L}_{\text{phys}} + \mathcal{L}_{\text{cost}} \)，固定\( \lambda_{\text{phys}} = 10^3 \)。
  - **训练流程**：
    - 双阶段：5,000步Adam（学习率1e-3） + L-BFGS（强Wolfe线搜索）。
    - 配点\( N_c=100 \)均匀分布。
    - 自动微分计算高阶导数（`create_graph=True`）。
    - 网络：4隐藏层×32神经元，tanh激活，Glorot均匀初始化。

## 3. 实验设计：数据集、基准、对比方法

- **数据集/场景**：无真实数据，完全基于ODE和约束的合成问题。参数见表1（m=k=c=1, T=5, x0=1, v0=0, r=0.1）。
- **基准**：以PMP打靶解为黄金参考（终端残差~1e-15），直接转录NLP解为第二参考（终端残差~1e-20）。两者成本相对误差约1.3e-3。
- **对比方法**：
  - PMP单次打靶法（高精度自适应Runge-Kutta积分）
  - 直接转录（梯形配点，N=100，SLSQP求解NLP）
  - 本文PINN（双网络，硬边界，λ_phys=1e3）
- **评估方式**：所有方法在公共的密集参考网格（501均匀点）上计算成本、L2和L∞点态误差、终端约束残差。

## 4. 资源与算力

- **计算平台**：Google Colab CPU运行时（未说明GPU型号或数量；文中提到“CUDA backend when available”，但实际训练在CPU上完成）。
- **训练时间**：
  - PMP打靶：约1.2秒
  - 直接转录NLP：约10.4秒
  - PINN总训练：125.0秒（Adam阶段79.2秒 + L-BFGS阶段45.8秒）
- **其他资源**：未报告额外算力消耗如内存、分布式训练等。代码以单一Jupyter Notebook形式公开，可在Colab上完整复现。

## 5. 实验数量与充分性

- **实验组数**：
  - 仅针对一组固定参数（表1）进行了详细对比，未包含不同系统参数、不同权重r、不同边界条件或不同网络架构的消融实验。
  - 仅使用一个随机种子，未报告多种子统计结果（但指出“qualitative behaviour is robust across seeds”，未提供数据）。
  - 未研究非均匀配点、自适应权重或不同激活函数等。
- **充分性与公平性**：
  - 充分性有限：实验设计聚焦于单一基准的可复现性，未覆盖方法鲁棒性、泛化能力、敏感性分析等典型消融。
  - 公平性较好：提供了两种经典参考线，PINN结果与经典解直接对比，且误差水平与经典方法间差异相当。训练时间如实报告，未回避慢速劣势。
  - 客观性：承认PINN在本题上不具竞争力，既展示了优势（硬边界、精度达四位有效数字）也指出不足（训练慢、控制误差大）。

## 6. 论文的主要结论与发现

- PINN能够以四位有效数字再现经典最优成本，精确满足终端状态约束（硬边界强制）。
- 点态位移误差约10^-4~10^-3 m，速度误差约10^-3 m/s，控制误差约10^-2 N，均落在两种经典参考解的散布范围内。
- 误差层次：直接近似量（位移）最精确，导数量（速度）次之，独立近似量（控制）最差；控制误差在初始时刻最大。
- 训练时间比经典打靶慢两个数量级，比转录NLP慢一个数量级，反映了PINN在当前小规模线性问题上的计算低效。
- 主要贡献在于方法论清晰性和可复现性，而非算法创新。附带Colab Notebook和CSV数据，便于教育和扩展开源。

## 7. 优点

- **教学导向与可复现性**：提供了完整、可独立运行的Google Colab实现，设置固定随机种子，所有数值结果和图表均可一键复现。
- **透明且诚实的比较**：同时使用两种经典方法作为参考，点态误差详细报告，并明确指出现PINN训练时间为经典方法的两个数量级，不夸大性能。
- **硬边界强制技术**：利用三次多项式+二次掩膜的复合ansatz自动满足所有边界条件，避免软约束的调参困难，且保证终端约束恒满足，是一个实用技巧。
- **损失权重效应分析**：清晰展示了如果不加权重（λ_phys=1）导致物理残差过大、成本虚低的问题，强调了残差平衡的必要性。
- **代码公开与数据存档**：所有轨迹数据、损失历史以CSV保存，并提供永久DOI（Zenodo），便于第三方验证和二次利用。

## 8. 不足与局限

- **实验覆盖严重不足**：
  - 仅测试一组参数，未改变系统质量、弹簧、阻尼系数或权重r。
  - 未进行多种子统计，无法评估训练方差。
  - 无消融实验（如网络深度/宽度的影响、动态权重策略、不同ansatz设计、不同配点数量等）。
- **方法局限**：
  - 仅适用于线性、低维、平滑的最优控制问题；对于非线性、高维、含不等式约束或复杂终端成本的问题未作探究。
  - 控制网络无边界强制，导致控制误差较大（尤其在初始时刻）。
  - 损失权重λ_phys通过“少数预训练”手动固定，未采用自适应策略，可能导致泛化问题。
  - 训练使用固定均匀配点，未探索非均匀或自适应配点。
- **偏差风险**：
  - 只在一种容易求解的线性二次型上验证，结论可能过度乐观。作者自身承认该基准是“classical methods already at their most efficient”，PINN的优势并未体现。
  - 未报告收敛稳定性或对超参数（学习率、层宽）的敏感性。
- **应用限制**：直接PINN方法目前难以替代经典方法于高效计算场景；其潜在优势（如处理逆问题、部分观测、端到端可微）未被当前实验验证。
- **算力报告不完整**：未提及GPU型号、显存使用、是否启用批处理等细节，限制了算力对比的精确性。

（完）
