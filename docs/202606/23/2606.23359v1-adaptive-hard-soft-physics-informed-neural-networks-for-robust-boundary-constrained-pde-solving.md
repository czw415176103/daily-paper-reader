---
title: Adaptive Hard-Soft Physics-Informed Neural Networks for Robust Boundary-Constrained PDE Solving
title_zh: 自适应硬-软物理信息神经网络用于鲁棒边界约束偏微分方程求解
authors: "Duc Tien Nguyen, Trinh Minh Tuan, Nguyen Duc Manh, Vu Linh Nguyen, Dinh Gia Ninh"
date: 2026-06-22
pdf: "https://arxiv.org/pdf/2606.23359v1"
tags: ["query:sr-piml"]
score: 9.0
evidence: 物理信息神经网络求解偏微分方程
tldr: 传统PINN以软约束处理边界条件，导致收敛慢、精度低且对损失权重敏感。为此提出自适应硬-软物理信息神经网络（HSPINN），通过解析提升、掩码函数或周期特征映射精确强制执行Dirichlet和周期边界条件，而PDE残差、Neumann通量和初始条件作为软约束，并采用逆共享softmax动态平衡各损失分量。在Poisson、Burgers和周期对流问题中，HSPINN较传统PINN收敛更快、精度更高、稳定性更强。该工作为物理约束深度学习建立了通用可扩展的框架。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.23359v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1558, \"height\": 726, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.23359v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1563, \"height\": 1026, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.23359v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1621, \"height\": 1313, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.23359v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1596, \"height\": 1061, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.23359v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1625, \"height\": 1303, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.23359v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1599, \"height\": 1050, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.23359v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1622, \"height\": 1310, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.23359v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1661, \"height\": 327, \"label\": \"Table\"}]"
motivation: 传统PINN因软约束和损失权重调参导致边界不准确、收敛慢且梯度不稳定。
method: 通过解析提升或多项式构造精确满足Dirichlet和周期边界，软约束处理PDE残差等，逆共享softmax自适应调整损失权重。
result: 在Poisson、Burgers和周期对流问题中，HSPINN比传统PINN收敛更快、精度更高、稳定性更强。
conclusion: HSPINN为物理约束深度学习提供了通用可扩展的框架。
---

## 摘要
物理信息神经网络通过将物理原理嵌入学习过程，为求解偏微分方程提供了一种有效方式。然而，传统PINN将所有约束作为复合损失中的软惩罚项，常因优化地形病态而表现出收敛缓慢、对损失权重缩放敏感以及边界约束不准确等问题。为克服这些限制，本研究提出了一种带有自适应损失加权的统一硬-软物理信息神经网络（HSPINN）。在该框架中，通过解析或多项式提升、掩蔽函数和周期特征映射精确强制执行狄利克雷和周期边界条件，而控制方程残差、诺伊曼通量和初始条件则作为软约束处理。采用反向共享softmax策略在训练过程中动态平衡各损失分量相对重要性，免除了手动惩罚调整并改善了梯度稳定性。该公式确保了优化过程中边界可接受性，并提升了收敛效率和数值鲁棒性。应用于代表性椭圆型（泊松）、抛物型（伯格斯）和双曲型（周期边界对流）问题的结果表明，HSPINN相比传统PINN始终实现更快收敛、更高精度和更好稳定性，为科学技术领域的物理约束深度学习奠定了通用可扩展基础。

## Abstract
Physics-informed neural networks (PINNs) provide an effective way to solve partial differential equations (PDEs) by embedding physical principles into the learning process. However, the conventional PINN formulation, in which all constraints are imposed as soft penalty terms within a composite loss, often exhibits slow convergence, sensitivity to loss weight scaling, and inaccurate boundary enforcement due to poor conditioning of the optimization landscape. To address these limitations, this study proposes a unified hard--soft physics--informed neural network (HSPINN) with adaptive loss weighting. In this framework, Dirichlet and periodic boundary conditions are enforced exactly by construction through analytical or polynomial lifting, masking functions, and periodic feature mappings, while the governing PDE residuals, Neumann fluxes, and initial conditions are treated as soft constraints. An inverse-share softmax strategy dynamically balances the relative importance of individual loss components during training, eliminating manual penalty tuning and improving gradient stability. This formulation ensures boundary admissibility throughout optimization and enhances convergence efficiency and numerical robustness. Applications to representative elliptic (Poisson), parabolic (Burgers), and hyperbolic (convection with periodic boundaries) problems demonstrate that HSPINN consistently achieves faster convergence, higher accuracy, and greater stability than conventional PINNs, establishing a general and scalable foundation for physics-constrained deep learning across science and technology.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **研究动机**：传统物理信息神经网络（PINNs）在求解偏微分方程时，将所有约束（包括边界条件、初始条件）作为软惩罚项加入复合损失函数。这种做法存在严重弊端：
  - 收敛缓慢，对损失权重缩放高度敏感；
  - 边界条件得不到精确满足，尤其是狄利克雷边界和周期边界；
  - 优化地形病态，梯度不稳定，甚至出现损失不平衡导致训练失败。
- **整体含义**：本文提出一种统一的“硬-软”约束框架（HSPINN），旨在从根本上提升PINNs的边界精度、收敛速度和数值鲁棒性，为科学机器学习提供更通用的求解器。

## 2. 方法论：核心思想、关键技术细节

- **核心思想**：将部分已知约束（狄利克雷边界和周期边界）通过网络架构**精确强制执行**（硬约束），而将PDE残差、诺伊曼通量和初始条件作为**软约束**通过损失函数最小化；同时采用自适应损失加权策略自动平衡各分量。
- **关键技术细节**：
  1. **硬约束实现**：
     - 对于狄利克雷边界：构造提升函数 \(h_D\)（满足 \(h_D|_{\Gamma_D}=g_D\)）和掩码函数 \(m_D\)（在边界上为零，内部为正），网络输出定义为 \(u_\theta = h_D + m_D \cdot v_\theta\)，其中 \(v_\theta\) 是可训练神经网络。这样任何 \(v_\theta\) 都保证边界条件精确满足。
     - 对于周期边界：采用周期特征映射，例如将输入 \(x\) 映射为 \(\sin(2\pi x/L), \cos(2\pi x/L)\)，使得输出自动具有周期性。
  2. **软约束**：PDE残差、诺伊曼通量、初始条件以均方误差形式构成损失函数，不含狄利克雷惩罚项。
  3. **自适应损失加权**：采用逆共享softmax（inverse-share softmax）策略，基于每个损失分量的梯度范数（或损失量级）动态计算权重，自动平衡各损失贡献，消除手动调参。
  4. **训练流程**：两阶段优化——第一阶段用Adam随机热身（探索），第二阶段用L-BFGS确定性细化（局部收敛）。网络最后一层初始化为零，确保初始输出即满足边界。
- **算法流程**（文字说明）：
  - 输入：区域、PDE算子、源项、边界数据、提升函数、掩码函数、网络架构、采样数量。
  - 网络初始化：Xavier初始化，最后线性层置零。
  - 定义硬约束输出：\(u_\theta = h_D + m_D v_\theta\)。
  - 循环Adam阶段：每步采样新配点，计算各残差与部分损失，用逆共享softmax计算自适应权重，组成总损失，反向传播更新。
  - 循环L-BFGS阶段：在固定或定期刷新的配点集上执行强Wolfe线搜索的L-BFGS迭代。

## 3. 实验设计：数据集/场景、基准、对比方法

- **数据集/场景**：三类代表性PDE
  - 椭圆型：二维泊松方程（\(-\Delta u = f\)），混合边界（左/下狄利克雷，右/上诺伊曼），解析解 \(u = \sin(\pi x)\sin(\pi y)\)。
  - 抛物型：一维粘性伯格斯方程（\(u_t + u u_x - \nu u_{xx} = f\)），\(\nu=0.01\)，非零狄利克雷边界 \(u(0,t)=u(1,t)=1\)，解析解 \(u = 1 + \sin(\pi x)e^{-t}\)。
  - 双曲型：纯对流方程（\(u_t + \beta u_x = 0\)），周期边界，解析解 \(u = \sin(x - \beta t)\)。
- **基准**：所有问题均有精确解析解，可计算相对 \(L_2\)、\(L_\infty\) 误差和边界最大误差。
- **对比方法**：常规软PINN（SPINN），损失权重经手动调节以达到最佳效果。网络架构、优化器设置均相同：5层隐藏层，每层64神经元，tanh激活。
- **评价指标**：相对 \(L_2\) 误差、\(L_\infty\) 误差、边界最大误差。

## 4. 资源与算力

- 论文未明确说明使用的GPU型号、数量或具体训练时长（仅给出了不同问题的耗时比较表，但未说明GPU类型）。仅在训练细节中提到使用单精度（torch.float32）在单个GPU上训练。损失函数计算与反向传播采用自动微分（PyTorch）。因此无法准确评估资源开销的具体规模。

## 5. 实验数量与充分性

- **实验组数**：共3个主要实验（泊松、伯格斯、对流），每个实验均对比HSPINN与SPINN，展示预测场、误差分布、训练收敛曲线、自适应权重演化。此外，提供了一个汇总表（表1）对比训练时间和最终误差。
- **消融实验**：未单独设计消融实验（例如去掉自适应加权、去掉硬约束等），但通过与纯软PINN对比间接体现了各模块效果。
- **充分性评价**：实验覆盖了椭圆、抛物、双曲三大类PDE，边界类型涵盖了狄利克雷、诺伊曼、周期，具有较好的代表性。但缺少对更复杂几何（不规则区域）、高维问题、向量场系统、逆问题等的测试。此外，未进行多次随机重复实验（可能仅单次运行），统计显著性未讨论。对比中SPINN的权重经过手动调优，但不能保证绝对公平。总体实验较充分，但仍有提升空间。

## 6. 主要结论与发现

- HSPINN在所有测试问题中均获得比传统软PINN更快的收敛速度、更高的精度（相对 \(L_2\) 误差降低1~2个数量级）和更强的稳定性。
- 狄利克雷边界被精确满足至机器精度，周期边界由架构保证完美执行。
- 自适应加权策略自动平衡各损失分量，消除手动调参，改善了梯度稳定性。
- 训练时间减少约1.3~3.2倍，同时误差更低（例如泊松问题误差从 \(1.52\times10^{-5}\) 降至 \(1.02\times10^{-7}\)；伯格斯问题误差从 \(5.26\times10^{-6}\) 降至 \(7.62\times10^{-8}\)；对流问题误差从 \(3.02\times10^{-2}\) 降至 \(5.75\times10^{-7}\)）。
- HSPINN在不增加参数或改变优化器的前提下获得上述提升，归功于缩小假设空间和改善损失地形条件。

## 7. 优点

- **架构创新**：统一处理狄利克雷和周期边界为硬约束，诺伊曼和初始条件为软约束，无需几何依赖的距离函数，实现简单、计算高效。
- **自适应加权**：逆共享softmax自动平衡损失，无需手动调参，适用于多目标优化，提高训练稳定性。
- **两阶段训练**：Adam+L-BFGS结合，兼顾探索与局部精细收敛，且初始化策略保障边界可行性。
- **通用性**：在椭圆、抛物、双曲三类PDE上均表现出显著优势，且与自动微分和标准深度学习框架完全兼容。
- **性能提升显著**：收敛速度和最终精度均大幅提升，且无额外计算开销。

## 8. 不足与局限

- **实验覆盖有限**：仅测试了简洁几何（矩形、一维区间）上的标量PDE，未涉及不规则区域、高维问题、向量场系统（如纳维-斯托克斯）或逆问题。
- **消融分析缺失**：未单独验证硬约束与自适应加权各自贡献，也未比较不同加权策略（如梯度归一化 vs. 软注意 vs. 不确定性加权）之间的性能差异。
- **统计可靠性**：未报告多次重复实验的均值和方差，单次结果可能存在随机性，结论的鲁棒性有待进一步验证。
- **对比公平性**：SPINN的权重经手动调优，但调优过程可能不完美，且未尝试其他先进基线（如XPINN、自适应PINN、因果PINN等）。
- **计算资源描述不充分**：未说明GPU型号和具体训练环境，影响可复现性。
- **实际应用限制**：硬约束依赖于解析提升函数和掩码函数，对于复杂边界几何需要构造合适的函数，可能增加实现复杂度；周期边界通过特征映射实现，但仅适用于规则周期，无法直接处理非规则周期性。

（完）
