---
title: Accelerating physics-informed neural networks for full waveform inversion using a hybrid quantum-classical finite-basis architecture
title_zh: 利用混合量子-经典有限基架构加速用于全波形反演的物理信息神经网络
authors: "Hoang Anh Nguyen, Divakar Vashisth, Ali Tura"
date: 2026-05-31
pdf: "https://arxiv.org/pdf/2606.01110v1"
tags: ["query:symbolic-reg"]
score: 9.0
evidence: 物理信息神经网络用于PDE求解
tldr: "全波形反演计算成本高，物理信息神经网络收敛困难。本文提出混合量子-经典FBPINN，将波场和速度网络实现为参数化量子电路，支持端到端自动微分。在地球物理异常基准上，量子混合方法在约1/8训练迭代次数下达到更低的L1速度误差，且参数少33%。该框架可推广至医学超声断层扫描等波动反演问题。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.01110v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1470, \"height\": 697, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.01110v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1667, \"height\": 788, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.01110v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 945, \"height\": 1893, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.01110v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1644, \"height\": 473, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.01110v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1134, \"height\": 1092, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.01110v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1308, \"height\": 869, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.01110v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1225, \"height\": 260, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.01110v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1659, \"height\": 1072, \"label\": \"Table\"}]"
motivation: 为降低全波形反演的计算负担并克服物理信息神经网络的收敛难题，利用量子计算加速。
method: 提出混合量子-经典FBPINN，波形和速度子网络均采用参数化量子电路，通过JAX可微分模拟器实现端到端训练。
result: "在地球物理异常基准上，量子混合方法相比经典基线L1误差更低，训练迭代减少约8倍，可训练参数减少33%。"
conclusion: 该混合架构是通用的，可扩展至医学超声断层扫描等波动反演问题。
---

## 摘要
全波形反演（FWI）通过接收器数据重建非均匀材料属性，但计算成本高昂。物理信息神经网络（PINNs）及其域分解变体（FBPINNs）提供了一种无网格替代方案，但在表示复杂速度场时面临收敛挑战。我们提出了一种用于声波FWI的混合量子-经典FBPINN，将量子计算与经典机器学习相结合，其中分解波场网络和全局速度网络被实现为经典到量子管道，终止于参数化量子电路（PQCs）。PQCs被实现为可微分的JAX态矢量模拟器，使得通过经典PINN、量子电路和物理信息损失进行端到端自动微分成为可能。在地球物理异常基准测试中，尽管使用的可训练参数减少了约33%，量子混合模型在训练迭代次数减少约8倍的情况下达到了比经典FBPINN基线更低的L1速度误差，并且优于所有测试的15个经典超参数变体。第二个基准测试（棋盘格）证明了反演管道的通用性，确认了量子混合架构能够恢复超出局部异常基准的结构化空间变化。我们的框架广泛适用于地球物理学之外的基于波的反问题，包括医学超声断层扫描和无损评估。

## Abstract
Full waveform inversion (FWI) reconstructs heterogeneous material properties from receiver data but remains computationally demanding. Physics-informed neural networks (PINNs) and their domain-decomposed variants (FBPINNs) offer a mesh-free alternative but face convergence challenges when representing complex velocity fields. We present a hybrid quantum-classical FBPINN for acoustic FWI, bringing together quantum computing and classical machine learning, in which the decomposed wavefield network and the global velocity network are implemented as classical-to-quantum pipelines terminating in parameterized quantum circuits (PQCs). The PQCs are realized as differentiable JAX statevector simulators, enabling end-to-end automatic differentiation through the classical PINN, the quantum circuit, and the physics-informed loss. On a geophysical anomaly benchmark, the quantum hybrid reaches a lower L1 velocity error than the primary classical FBPINN baseline in approximately 8x fewer training iterations, despite using approximately 33% fewer trainable parameters, and it outperforms all 15 classical hyperparameter variants tested. A second benchmark (checkerboard) demonstrates the generality of the inversion pipeline, confirming that the quantum hybrid architecture can recover structured spatial variations beyond the localized anomaly benchmark. Our framework is broadly applicable to wave-based inverse problems beyond geophysics, including medical ultrasound tomography and non-destructive evaluation.

---

## 论文详细总结（自动生成）

好的，作为一名资深学术论文分析助手，我将根据您提供的论文内容和格式要求，为您生成一份结构化的、深入且客观的中文总结。

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**: 全波形反演 (FWI) 是地球物理、医学成像等领域的关键技术，但其迭代求解波动方程的计算成本极高。物理信息神经网络 (PINNs) 及其实域分解变体 (FBPINNs) 提供了一种无网格的替代方案，但在表示复杂速度场时面临**收敛速度慢**和**精度不足**的挑战。
- **研究动机**: 探索将**量子计算**的潜力引入 FWI，以解决经典 PINNs 方法的收敛瓶颈。特别是，利用参数化量子电路 (PQCs) 作为神经网络中的函数逼近器，期望能利用其高维表达能力和参数效率来加速训练并提高反演精度。
- **整体含义**: 本文首次将门级混合量子-经典神经网络架构应用于高度非线性的FWI问题。该工作不仅为地球物理反演提供了新思路，其提出的通用型框架也适用于医学超声断层扫描、无损检测等其他基于波的反问题，展示了量子计算在解决复杂科学计算问题中的潜力。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**: 提出一种**混合量子-经典 FBPINN** 架构。该架构将 FBPINN 框架中的波场网络和速度网络，都替换为 **“经典到量子”** 的管道。具体地，经典子网络将输入坐标映射为低维潜在特征，然后由参数化量子电路 (PQC) 进行处理，最后通过经典输出层输出物理量。这样做的目的是结合经典神经网络的灵活性与量子电路的潜在表达能力。
- **关键技术细节**:
    1.  **域分解 (FBPINN)**: 将整个计算域划分为多个重叠的子域，每个子域由一个独立的网络表示。这缓解了标准 PINN 的“频谱偏差”问题，有利于处理高频波场。
    2.  **参数化量子电路 (PQC)**: 采用了一种特定的PQC结构（**图2**），包括：
        - **角度编码层**: 将经典网络的输出通过 `Ry(βᵢzᵢ)` 旋转门编码到量子比特上，其中 `βᵢ` 是可训练的缩放参数。
        - **变分层**: 由 `Rx`, `Ry`, `Rz` 单比特旋转门和线性 CNOT 纠缠门组成的循环层（重复L次），这些门具有可训练的旋转角度 `θ`。
        - **测量层**: 测量每个量子比特的 Pauli-Z 期望值并取平均，输出一个在 [-1, 1] 区间的标量。
    3.  **可微分模拟**: PQC 通过 **JAX 库实现的状态向量模拟器**进行模拟。这使得整个模型（从经典网络、量子电路到物理损失函数）是**端到端可微的**，可以直接使用 JAX 的自动微分（`jax.grad`）进行训练，避免了硬件上的参数平移规则（parameter-shift rule）带来的巨大计算开销。
    4.  **速度网络约束**: 速度网络的输出通过 `tanh` 函数映射到一个有界范围 `[α_bg - A, α_bg + A]`，并乘以一个平滑空间掩码，从而将速度值限制在物理合理的范围内，起到正则化作用。
    5.  **联合训练**: 所有参数（经典网络权重、编码缩放参数、电路旋转角度、输出层权重）通过 Adam 优化器联合优化，损失函数综合了 PDE 残差、初始条件、数据拟合和边界条件等多个物理约束。

### 3. 实验设计：数据集、基准与对比方法

- **数据集/场景**: 使用两个二维声波传播基准模型：
    - **异常基准 (Anomaly)**: 一个局部低速异常体模型（约2.5 km/s 嵌入在约3.0 km/s 背景；图3a），由单侧接收器阵列记录波形数据。这是一个严重不适定的反问题。
    - **棋盘格基准 (Checkerboard)**: 一个3×3的棋盘格速度模型（图4a），用于测试方法恢复更复杂空间结构的能力。
- **基准 (Benchmark)**: **经典 FBPINN** 基线（纯粹的经典全连接网络，容量为151,836个参数）是主要的性能对照基准。
- **对比方法**: 除了主要基线和提出的量子混合模型外，实验还包含了**15个经典 FBPINN 超参数变体**（见补充表S1）。这些变体探索了不同的网络大小、子域划分、学习率和物理损失权重，旨在验证量子混合模型的优势是否来自于经典模型调参不足。

### 4. 资源与算力

- **文中未明确说明**: 论文未提及所使用的 GPU 型号、数量和具体的训练时长（wall-clock time）。作者明确指出，其性能比较基于“优化迭代次数”（iteration count），而非实际的运行时间，因为经典和量子架构的单次迭代成本不同。
- **提及的算力相关细节**: 训练使用了 JAX 的即时编译（JIT）和向量化批处理，将整个更新步骤编译成一个单一的 XLA 程序在 GPU 上运行。对于量子混合模型，4量子比特的 PQC 状态向量模拟的显存占用（2⁴=16个复数振幅）与经典网络相比几乎可以忽略不计。

### 5. 实验数量与充分性

- **实验数量**: 比较充分。
    - 在异常基准上进行了16组对比实验（1个主要基线 + 15个超参数变体 + 1个量子混合模型）。
    - 在棋盘格基准上，使用与异常基准完全相同的网络结构（仅增加配置点密度）进行测试，验证了方法的通用性。
    - 进行了梯度数值验证（与 PennyLane 对比，最大偏差 <2×10⁻⁷）。
- **充分性与公平性**:
    - **优点**: 15个经典超参数变体的广泛扫描，使得结论“量子混合模型优于所有经典变体”具有较强的说服力，排除了经典模型调参不佳带来的偏差。
    - **不足**: 作者坦承，对比**并非参数匹配**（parameter-for-parameter）。即，量子混合模型的总参数更少，但其经典骨干网络的宽度和深度也与经典基线不同。因此，对比结果反映的是整个“经典到量子”替代的效果，而非纯粹的量子电路优势。
    - **缺失的关键对照**: 实验缺乏一个使用**显式傅里叶特征映射**或**正弦激活函数（SIREN）** 的经典 FBPINN 作为控制实验。这使得无法区分观测到的性能提升是来自于 PQC 特有的诱导偏差（有限频率结构），还是仅仅来自一个更好的三角函数基函数。

### 6. 论文的主要结论与发现

1.  **性能显著提升**: 在异常基准上，量子混合模型相比经典 FBPINN 基线，在**训练迭代次数减少约8倍**（65k vs 500k 次迭代）的情况下，取得了**更低**的 L1 速度误差（2.4×10⁻² vs 2.9×10⁻²）。
2.  **参数效率更高**: 量子混合模型实现上述性能，使用的可训练参数**减少了约33%**（101,812 vs 151,836）。
3.  **泛化能力**: 量子混合模型在棋盘格基准上成功恢复了更多结构化空间变化，证明了其通用性。
4.  **超越经典调参**: 量子混合模型优于所有15个经典超参数变体，表明其优势并非源于经典基线选择不当。

### 7. 优点：方法与实验设计上的亮点

- **新颖的混合架构**: 将 PQC 与 FBPINN 域分解相结合，用于解决高度非线性的 FWI 反问题，这是之前工作未涉及的。
- **端到端可微分实现**: 通过 JAX 实现 PQC 状态向量模拟器，实现了经典网络、量子电路和物理损失的联合自动微分，提高了训练效率和稳定性，为量子-经典混合方法在科学计算中的应用提供了重要范例。
- **严谨的实验验证**: 进行了大规模的超参数扫描（15个变体），这显著增强了核心实验结论的可信度。
- **开源精神**: 提供了代码和数据仓库，便于研究的复现和扩展。
- **诚实的自我批评**: 作者在讨论部分非常诚实地分析了性能提升的可能来源、实验的局限性以及缺失的关键对照实验，这种科学态度值得肯定。

### 8. 不足与局限

- **实验覆盖有限**:
    - 仅在**2D 声波**且**单震源**配置下进行了测试，未扩展至更实际的三维弹性波或多震源问题。
    - 缺乏**参数匹配**的经典控制实验，特别是**傅里叶特征**或 **SIREN** 网络，这使得量子电路本身带来的特定优势（如通过纠缠捕获特征间相关性）无法被有效分离。
    - 所有性能提升都基于**优化迭代次数**，而非实际**运行时间**。由于 PQC 状态向量模拟的开销，在实际运行时间上可能没有优势。
- **核心机制未完全理解**: 作者承认，尽管推测了 PQC 诱导的有限傅里叶结构、参数效率等是性能提升的原因，但三者与更快的收敛率之间的因果关系**尚未被证明**。收敛加速的机制仍是个待解决的开放性问题。
- **硬件局限性**:
    - 当前实验依赖**经典状态向量模拟**，可扩展性受限于 `O(2^n)` 的内存开销（量子比特数 n）。
    - 若迁移到真实 NISQ 量子硬件，将面临**参数平移规则的计算开销**、**设备噪声**以及**贫瘠高原（barren plateaus）** 等问题，可能会侵蚀在模拟器中观察到的优势。
- **潜在偏差风险**: 速度网络初始化的L1误差起点不同，虽然进行了说明，但仍可能引入细微的偏差。作者也承认“量子电路是经典可实现的函数类”，因此不声称“量子计算优越性”，定位准确。

（完）
