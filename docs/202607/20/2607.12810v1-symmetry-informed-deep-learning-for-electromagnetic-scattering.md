---
title: Symmetry-Informed Deep Learning for Electromagnetic Scattering
title_zh: 对称性引导的电磁散射深度学习
authors: "Viktor A. Lilja, Philippe Tassin"
date: 2026-07-14
pdf: "https://arxiv.org/pdf/2607.12810v1"
tags: ["query:physics-ml"]
score: 8.0
evidence: 利用麦克斯韦方程等变性的对称性信息深度学习
tldr: 电磁模拟的高成本限制了深度学习代理模型的数据效率。本文利用麦克斯韦方程组的等变性，推导出器件对称性到散射参数变换的通用规则，从而进行数据增强并构建严格等变的神经网络。在光子晶体板和自由形式衍射光栅上验证，该方法相比标准架构将数据效率提升一个数量级，且等变模型能强制满足物理约束。对称性作为一种通用归纳偏置，为计算电磁学及其他领域的数据高效、物理一致的学习建立了第一性原理框架。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.12810v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 846, \"height\": 507, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.12810v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 890, \"height\": 414, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.12810v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 882, \"height\": 823, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.12810v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 881, \"height\": 451, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.12810v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1766, \"height\": 475, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.12810v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 892, \"height\": 522, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.12810v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 879, \"height\": 742, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.12810v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 904, \"height\": 357, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.12810v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 907, \"height\": 507, \"label\": \"Table\"}]"
motivation: 深度学习电磁模拟面临数据效率瓶颈，因训练数据依赖昂贵数值模拟，亟需利用对称性减少数据需求。
method: 基于麦克斯韦方程等变性，推导器件对称性到散射参数的变换规则，实现数据增强和严格等变神经网络的构建。
result: 在光子晶体板和自由形式光栅上，数据效率比标准架构提升一个数量级，且等变模型精确满足物理约束。
conclusion: 对称性作为通用归纳偏置，为电磁学及其他领域提供了数据高效、物理一致的代理建模框架。
---

## 摘要
深度学习能够通过训练神经网络将设计参数映射到散射参数，从而替代成本高昂的仿真，加速电磁器件的建模。然而，数据效率仍是核心瓶颈，因为训练数据通常通过昂贵的数值仿真生成。本文表明，对称性为克服电磁散射问题中的这一限制提供了一条强大且尚未充分利用的途径。利用麦克斯韦方程组的等变性，我们推导出通用变换规则，将电磁器件的对称性映射为其散射参数的相应变换。这既实现了系统性的数据增强，也允许构建精确等变的神经网络。我们针对离散和连续对称群实现了该框架，并在光子晶体平板和自由形状衍射光栅上展示了其有效性。与标准架构相比，引入对称性将数据效率提高了一个数量级，而等变模型则能精确施加物理约束。我们的方法具有通用性，与现有的物理信息策略互补，为构建基于物理的代理模型提供了一个第一性原理框架，并将对称性确立为计算电磁学及更广领域中用于数据高效和物理一致学习的统一归纳偏置。

## Abstract
Deep learning can accelerate the modeling of electromagnetic devices by replacing costly simulations with neural networks trained to map design parameters to scattering parameters. However, data efficiency remains a central bottleneck, as training data is typically generated through expensive numerical simulations. Here we show that symmetry provides a powerful and largely untapped route to overcoming this limitation in electromagnetic scattering problems. Leveraging the equivariance of Maxwell's equations, we obtain general transformation rules that map symmetries of electromagnetic devices to corresponding transformations of their scattering parameters. This enables both systematic data augmentation and the construction of exactly equivariant neural networks. We implement the framework for both discrete and continuous symmetry groups and demonstrate its effectiveness on photonic-crystal slabs and free-form diffraction gratings. Incorporating symmetry improves data efficiency by an order of magnitude compared to standard architectures, while equivariant models additionally enforce physical constraints exactly. Our approach is general and complementary to existing physics-informed strategies, provides a first-principles framework for constructing physically grounded surrogate models, and establishes symmetry as a unifying inductive bias for data-efficient and physically consistent learning in computational electromagnetics and beyond.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）
- **核心问题**：深度学习在电磁器件建模中可作为高计算成本数值仿真（如FDTD、FEM）的代理模型，但训练数据的生成本身成本高昂，导致数据效率成为瓶颈。如何利用物理先验知识（如对称性）减少对大量标注数据的依赖，是亟待解决的关键问题。
- **整体意义**：本文指出，现有物理信息方法多关注不变性（即对称变换下响应不变），而忽视了更一般的等变性（即输入和输出都按规则变换）。作者系统性地利用麦克斯韦方程组的等变性，为电磁散射问题构建了对称性驱动的深度学习框架，显著提升数据效率，并保证预测的物理一致性。

## 2. 方法论：核心思想、关键技术细节
### 核心思想
- 从麦克斯韦方程在坐标变换下的形式不变性出发，证明设计参数到散射参数的映射是等变的：对材料分布施加对称变换（如旋转、反射、平移），散射参数按照特定规则变换。
- 利用这一等变性，可进行**数据增强**（无需额外仿真即可扩充样本），并构建**严格等变的神经网络**（架构本身满足对称约束）。

### 关键技术细节
- **理论推导**：
  - 考虑线性、局部、时不变材料，散射参数由端口模式展开定义。对于坐标变换 \( \hat{x} = T(x) \)，场和材料参数的变换由雅可比矩阵 \(\Lambda\) 决定（式(4)(5)）。
  - 重点考虑保长（欧几里得）变换（旋转、反射、平移），因为弯曲变换通常使得各向同性材料变为各向异性，超出设计空间。
  - 推导散射参数的变换规则（式(8)）：\( \hat{S} = \rho^+ S (\rho^-)^{-1} \)，其中 \(\rho^{\pm}\) 是端口模式变换后的重叠矩阵。这为数据增强提供了精确的标签变换公式。
- **数据增强**：对每个训练样本 \((d, S)\)，应用群 \(G\) 中的所有变换 \(g\)，生成新样本 \((g \triangleright d, g \triangleright S)\)，数据集大小扩大 \(|G|\) 倍。
- **等变神经网络构建**：
  - **离散群（如D6）**：使用等变MLP（EMLP），其权重矩阵满足约束 \(W_l = \rho_{l+1}(g) W_l \rho_l(g^{-1})\)（式(9)）。输入和输出特征赋予相应群的线性表示。
  - **连续群（如E(1)）**：使用等变CNN（ECNN），基于E(n)-steerable CNN框架。关键创新：由于散射参数 \(S\) 不直接作为特征场变换，作者提出了**等变读出层**（式(14)）：先执行核大小为1的群卷积，再利用表示加权全局池化，将特征场映射为散射参数，保证平移等变。

## 3. 实验设计：数据集、基准、对比方法
### 系统一：六角光子晶体平板（PhC）
- **参数化**：12个参数描述单胞内孔形状；输入端口为x/y偏振平面波，输出为琼斯矩阵 \(S = (S_{xx}, S_{xy}, S_{yy})^T\)。
- **对称群**：D6群（12个元素），由60°旋转和xz面反射生成。
- **数据集**：12,000个随机PhC样本（仿真工具未明，但提及使用特定仿真软件）。
- **对比方法**：
  - 标准MLP（基线）
  - MLP+数据增强（MLP-A）
  - 等变MLP（EMLP）
- **评估**：不同训练集大小下的测试损失（MSE），误差分布图，以及沿参数化路径上 \(S_{xy}\) 的预测误差（检验对称点处选择定则）。

### 系统二：一维自由形式衍射光栅
- **参数化**：32位二进制bitmap表示介质分布（1为材料，0为真空）。
- **输出**：三个衍射级次 \(S = (S_{-1}, S_0, S_{+1})^T\)。
- **对称群**：欧几里得群E(1)，包括平移（由周期 \(a\) 定义）和反射。
- **数据集**：24,000个随机光栅样本。
- **对比方法**：
  - 标准CNN（含池化、零填充、全连接读出层，破坏等变）
  - 等变CNN（ECNN，使用群卷积、周期性填充、空洞卷积代替池化、等变读出层）
  - 参考MLP
- **评估**：测试损失 vs 训练样本数；平移等变测试：对一个光栅样本，预测平移后的 \(S_{-1}\)，观察其在复平面上的轨迹是否成圆（理论要求）。

### 公平性保证
- 先固定模型宽度，再对基线模型（无增强）进行超参数优化（层数、batch normalization），然后从基线构造等变版本，确保参数量和推理时间相近（表II）。实验中每个配置使用六折交叉验证，重复6次取平均；超参数（学习率、权重衰减）针对每个模型和数据集大小重新优化。

## 4. 资源与算力
- **算力来源**：训练在瑞典国家基础设施NAISS的Chalmers/C3SE和KTH/PDC站点进行。未明确GPU型号、数量、总训练时长。
- **速度基准**（附录表II，单GPU）：
  - PhC：MLP训练0.86ms/batch，EMLP训练2.22ms/batch（batch size=128）。
  - 光栅：CNN训练3.80ms/batch，ECNN训练14.00ms/batch。
  - 等变模型训练更慢，但可能因收敛更快而减少总时间（附录F验证曲线）。

## 5. 实验数量与充分性
- **实验数量**：
  - 两个不同对称群和问题类型的系统。
  - 每个系统测试多个训练集大小（PhC图中约6个点，光栅图约5个点）。
  - 消融实验（附录表I）：对ECNN逐个移除等变组件（等变读出层→全连接读出、群卷积→普通卷积、池化替代空洞卷积、零填充替代周期填充），分别测试有无数据增强。
  - 训练动力学分析（附录F）：验证损失随epoch变化，并额外延长训练观察收敛。
- **充分性评估**：
  - **优点**：实验设计系统，公平性考虑充分（基线优化、交叉验证、重复）。消融研究揭示了各组件贡献（尤其零填充破坏性最大，池化在有增强时反而有益）。对称性测试直观。
  - **不足**：仅覆盖两个具体问题，未在更多样化的电磁器件（如三维结构、不同频段）上验证。未与其他物理信息方法（如PINN）直接比较。实验结果主要报告MSE，未关注相对误差或高Q值等关键应用指标。非等变模型的超参数可能并未达到最优（因为仅在基线模型上优化），但作者已尽力公平。

## 6. 主要结论与发现
- **数据效率提升一个数量级**：在PhC和光栅问题上，利用对称性（增强或等变）的模型在相同数据量下损失远低于标准模型，达到同等性能所需数据减少约10倍（图3(a)、图6(a)）。
- **数据增强提供大部分增益，等变网络进一步改善**：MLP-A与EMLP在PhC上性能相近（EMLP在大数据时略优）；在光栅上，ECNN在所有数据规模下损失最低，且等变性质带来额外好处。
- **等变网络精确满足物理约束**：在对称不变设计点上（如反射对称的PhC），EMLP自动预测 \(S_{xy}=0\)（图3(c)），而MLP和MLP-A仍有误差。对于光栅平移等变，ECNN预测的 \(S_{-1}\) 在复平面上严格按照理论呈完美圆形，而增强模型（CNN-A）也不能保证（图6(b)）。
- **对称性作为架构选择的第一性原理指南**：从通用MLP出发，施加等变约束自然导向合适的架构（如群卷积、等变读出），无需大量手动调参。

## 7. 优点
- **理论扎实**：从麦克斯韦方程组的变换性质出发，严格推导散射参数的变换规则（式(8)），为数据增强和等变架构提供了物理基础。
- **方法通用**：覆盖离散群（D6）和连续群（E(1)），可扩展到其他欧几里得对称性和维度；与现有的物理信息方法互补。
- **系统性实验验证**：在两种典型问题（参数化/自由形式）上验证，包含数据效率曲线、误差分布、对称性测试、消融分析，结果可信。
- **公平对比**：通过保持推理成本、超参数优化、交叉验证等方式力求公平。
- **实际贡献**：公开代码和数据集（GitHub），便于复现和推广。
- **额外发现**：等变网络在高对称点自动实现选择定则；等变读出层的设计（式(14)）为将非特征场输出映射为等变输出提供了通用方案。

## 8. 不足与局限
- **实验覆盖有限**：仅测试两个具体结构（2D光子晶体、1D光栅），未拓展到更复杂的三维结构、具有更丰富对称性的超表面或多端口器件。结果泛化能力需进一步验证。
- **未考虑方向反转变换**：如时间反演或反射导致输入/输出端口互换，此时散射矩阵变换涉及逆矩阵，非线性，不能用线性表示处理（附录I-2）。数据增强虽可行，但无法构建等变网络。
- **与其他物理信息方法的比较缺失**：未与PINN、Lorentz网络等现有方法对比，无法确定对称性相对优势的幅度。
- **超参数配置可能不公平**：虽然先优化基线再构造等变模型，但等变网络因参数量减少可能对学习率和weight decay更敏感；作者虽重新优化超参数，但未说明优化范围是否充分。
- **应用局限性**：方法针对散射参数预测，对于其他输出（如电场分布、色散关系）需相应调整。对于非欧几里得对称群（如晶体对称中的非点群）尚未处理。
- **算力消耗未充分报告**：未提供总训练GPU小时数，不利于他人评估成本。

（完）
