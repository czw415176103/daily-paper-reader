---
title: "Demonstrating TOFFEE: A Learned System for Synthesizing Data Agent Trajectories at Scale"
title_zh: 展示TOFFEE：一种大规模合成数据智能体轨迹的学习系统
authors: "Ziting Wang, Yin Li, Zuhao Yang, Xiuchang Li, Jiale Bai, Gao Cong"
date: 2026-07-07
pdf: "https://arxiv.org/pdf/2607.06233v1"
tags: ["query:cfd-agent"]
score: 8.0
evidence: 通过轨迹合成提升代理技能的方法
tldr: 现有LLM数据代理在异构企业环境中泛化能力不足，亟需高质量轨迹数据。TOFFEE系统采用蒙特卡洛树搜索、自适应模型选择和跨任务前缀重用来合成轨迹。实验表明，该方法能高效生成复杂分析任务的轨迹数据。该工作为数据代理的微调和上下文学习提供有力支持。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.06233v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 795, \"height\": 661, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.06233v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1454, \"height\": 539, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.06233v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 877, \"height\": 377, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.06233v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 791, \"height\": 784, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.06233v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 795, \"height\": 793, \"label\": \"Figure\"}]"
motivation: 现有LLM数据代理难以泛化到异构企业环境，需要高质量轨迹数据来提升其适应性和推理能力。
method: TOFFEE基于蒙特卡洛树搜索，结合自适应模型选择和跨任务前缀重用，系统化合成数据代理轨迹。
result: TOFFEE在异构环境中高效生成可扩展的轨迹数据，并成功用于代理微调和演示增强推理。
conclusion: TOFFEE为数据代理提供高质量轨迹合成方案，显著提升其在未知数据环境中的泛化性能。
---

## 摘要
基于大语言模型的数据智能体在数据驱动决策中扮演着越来越重要的角色。然而，现有数据智能体难以泛化到未见过的数据环境和分析工作流，特别是在异构企业环境中。这产生了一种日益增长的需求，即合成高质量的数据智能体轨迹，以捕捉给定数据环境中的复杂分析工作流。此类轨迹支持两种关键的后续用途：它们可以作为监督微调数据，使数据智能体模型适应目标领域；也可以作为上下文学习演示，指导通用大语言模型在陌生数据环境中工作。因此，我们引入TOFFEE，一种通过蒙特卡洛树搜索（MCTS）结合自适应模型选择和跨任务前缀复用来从给定数据环境合成高质量数据智能体轨迹的系统。我们展示了TOFFEE能够为跨异构环境的复杂分析任务有效生成可扩展的轨迹数据。在此演示中，我们介绍TOFFEE的系统框架，包括其任务池构建、轨迹探索器和学习代价模型。我们还介绍TOFFEE的Web界面及其工作流，并演示两个端到端场景：用于数据智能体微调的轨迹合成，以及演示增强的数据智能体推理。

## Abstract
LLM-powered data agents are playing an increasingly important role in data-driven decision making. However, existing data agents struggle to generalize to unseen data environments and analytical workflows, especially in heterogeneous enterprise settings. This creates a growing need for synthesizing high-quality data agent trajectories that capture complex analytical workflows for given data environments. Such trajectories support two key downstream uses: they can serve as supervised finetuning (SFT) data that adapts data agent models to the target domain, and as in-context learning (ICL) demonstrations to guide general-purpose LLMs in unfamiliar data environments. Thus, we introduce TOFFEE, a system for synthesizing high-quality data agent trajectories from given data environments via Monte Carlo Tree Search (MCTS) with adaptive model selection and cross-task prefix reuse. We show that TOFFEE can effectively generate scalable trajectory data for complex analytical tasks across heterogeneous environments. In this demonstration, we present the system framework of TOFFEE, including its task pool construction, trajectory explorer, and learned cost model. We also introduce the web interface of TOFFEE and its workflow, and demonstrate two end-to-end scenarios: trajectory synthesis for data agent finetuning, and demonstration-augmented data agent reasoning.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：基于 LLM 的数据智能体（data agent）在异构企业环境中泛化能力差，容易产生 SQL 错误、结论模糊等问题。
- **背景需求**：高质量的数据智能体轨迹（多步推理+工具调用序列）可以用于**监督微调（SFT）** 和**上下文学习（ICL）演示**，从而提升智能体对陌生数据环境的适应能力。然而，现有方法（如单次生成、Best-of-N 采样）要么浪费预算，要么无法探索错误恢复路径，难以规模化获取高质量轨迹。
- **整体含义**：TOFFEE 旨在解决轨迹合成的三大挑战——任务自动构建（C1）、海量搜索空间下的高效探索（C2）、每步推理配置的自适应选择（C3），从而高效生成可扩展的数据智能体轨迹，支撑下游智能体的训练与推理。

## 2. 论文提出的方法论

### 核心思想
TOFFEE 采用**蒙特卡洛树搜索（MCTS）** 作为搜索框架，结合**学习代价模型（LCM）** 动态选择每步的 LLM 配置，并通过**跨任务前缀复用**减少冗余计算，在给定数据环境中合成高质量轨迹。

### 关键技术细节
1. **任务池构建**  
   - 通过轻量脚本发现数据环境中的依赖关系（如多表共享字段、区域性波动），反向推导出多步分析任务。  
   - 每个任务都经过执行验证和确定性答案检查，确保可解且无数据泄漏。

2. **轨迹探索器**  
   - 基于 MCTS 的树搜索：节点代表分析状态，边代表工具调用（SQL/Python 等）。  
   - 允许分支到修复路径，保留错误-诊断-修复序列作为训练数据。  
   - 同一数据环境内的任务共享前缀操作（如 schema 发现），减少重复开销。

3. **学习代价模型（LCM）**  
   - 从当前状态提取特征（进度、schema 覆盖率、错误历史），打分各候选配置（LLM 模型、上下文长度、推理努力）。  
   - 结合预测收益 μ 和探索奖励 σ 选择配置，并根据分歧程度设置分支宽度 K。  
   - 通过在线强化学习（岭回归）持续更新参数，形成闭环反馈。

### 算法流程
1. 输入数据环境 → 构建任务池（包含执行验证的答案键）
2. 对每个任务：初始化搜索树，加载共享前缀  
3. 每一步：LCM 选择 LLM 配置和分支宽度 → 扩展节点并执行工具 → 计算奖励 → 反向传播更新 LCM  
4. 当确定性验证器确认输出与答案键一致时存储轨迹  
5. 导出轨迹用于 SFT 或 ICL

## 3. 实验设计

- **数据集/场景**：  
  - 轨迹合成阶段：使用用户连接的数据环境或现有基准（Spider、LiveSQLBench、SpreadsheetBench）  
  - 下游评估：**KramaBench** 和 **DSBench**（两个数据智能体基准）
- **对比方法**：  
  - 轨迹质量对比：Single-Pass 生成、Best-of-N 采样（N=5）、TOFFEE w/o LCM（无自适应模型选择）、TOFFEE  
  - 下游效果对比：OpenAI o3、Qwen3.5-27B（零样本）、Qwen3.5-27B + ICL（使用 TOFFEE 轨迹作为演示）、TOFFEE-27B（基于 Qwen3.5-27B 的 SFT 模型）
- **评价指标**：  
  - 轨迹质量：0~1 分，综合执行成功率、分析进展和最终答案与答案键的精确匹配  
  - 下游得分：KramaBench 和 DSBench 总体得分

## 4. 资源与算力

- **硬件**：Ubuntu 服务器，双 AMD EPYC 9555 CPU，8 块 NVIDIA H100 GPU  
- **API 调用**：通过 OpenRouter 调用 LLM（具体模型未明确，但包含成本层级）  
- **训练/合成时间**：论文未明确提及总时长或迭代次数，只给出了预算（per-task budget $0~$2）

## 5. 实验数量与充分性

- **实验数量**：主要展示了两个对比图：  
  - 图 3(a)：轨迹质量随多任务预算的变化（方法对比）  
  - 图 3(b)：下游基准得分（6 个模型/方法对比）  
  - 另提供了消融实验（TOFFEE w/o LCM）验证 LCM 的有效性。
- **充分性评价**：作为 demo 论文，实验初步证明了 TOFFEE 在合成质量和下游性能上的优势。但缺少更多数据集（如仅两个下游基准）、多次重复实验（未给出误差棒）、对轨迹质量的鲁棒性分析等。整体尚可支持核心主张，但充分性有限。

## 6. 论文的主要结论与发现

- TOFFEE 在轨迹合成质量上显著优于 Single-Pass 和 Best-of-N，且**在更低的预算下收敛到高质量**。  
- LCM 的自适应模型选择是提升合成效率的关键（TOFFEE w/o LCM 效果明显弱于 TOFFEE）。  
- 使用 TOFFEE 轨迹进行 ICL 即可提升 Qwen3.5-27B 的零样本表现；进行 SFT 后（TOFFEE-27B）**甚至超越了 OpenAI o3** 在 KramaBench 和 DSBench 上的得分。  
- 证明了轨迹合成对数据智能体泛化能力的重要价值。

## 7. 优点

- **自动任务构建**：通过数据依赖发现和验证生成可解任务，避免手动标注和无效问题。  
- **高效探索**：MCTS 配合修复路径，自然产生错误恢复样本，丰富训练数据多样性。  
- **预算感知的自适应配置**：LCM 动态调整模型和分支宽度，在保证质量的同时降低 API 成本。  
- **通用性**：轨迹可同时支持 SFT 和 ICL，适用于多种下游智能体架构。  
- **界面友好**：提供 Web 演示，支持实时树可视化、质量-成本追踪等交互功能。

## 8. 不足与局限

- **实验覆盖有限**：仅使用了两个下游基准（KramaBench、DSBench），未在更多真实企业场景中验证。  
- **对比不充分**：下游只对比了 OpenAI o3 和 Qwen 系列，未对比其他前沿模型（如 Claude、Gemini）或专门的数据智能体系统。  
- **轨迹质量定义较窄**：最终分数依赖确定性答案键，可能忽略开放性分析任务的灵活性。  
- **计算成本**：虽然优化了，但仍需依赖收费 API，大规模合成可能产生可观费用。  
- **泛化性未讨论**：尚未分析轨迹在不同数据类型（如图、非结构化文本）上的效果。  
- **泄漏风险**：论文声称通过执行隔离避免评估污染，但未提供正式证明或检测方法。

（完）
