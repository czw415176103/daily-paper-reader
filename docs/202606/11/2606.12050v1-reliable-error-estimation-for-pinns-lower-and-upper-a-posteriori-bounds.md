---
title: "Reliable Error Estimation for PINNs: Lower and Upper A Posteriori Bounds"
title_zh: PINNs的可靠误差估计：后验下界与上界
authors: "Ismail Huseynov, Arzu Ahmadova, Agamirza Bashirov"
date: 2026-06-10
pdf: "https://arxiv.org/pdf/2606.12050v1"
tags: ["query:sr-piml"]
score: 8.0
evidence: 物理信息神经网络的先验误差界
tldr: 物理信息神经网络(PINN)求解微分方程时缺乏可靠的误差下界，导致无法形成完整的误差包络。本文针对常微分方程(ODE)，在局部强单调性条件下导出可计算的后验下界，并结合比全局Lipschitz更弱的单侧Lipschitz条件给出局部上界，形成双向误差证书。对于线性时不变/时变系统，推导了基于系统矩阵对称部分特征值的显式公式。该框架提供了严格且实用的误差认证，并讨论了初值条件软硬约束对下界有效性的影响。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.12050v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1201, \"height\": 470, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.12050v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1272, \"height\": 467, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.12050v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1097, \"height\": 415, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.12050v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1096, \"height\": 436, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.12050v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1234, \"height\": 443, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.12050v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1231, \"height\": 444, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.12050v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 930, \"height\": 470, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.12050v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1032, \"height\": 372, \"label\": \"Table\"}]"
motivation: 现有PINN误差仅有上界，缺少下界导致无法获得完整的误差包络，阻碍了模型的可靠认证。
method: 在局部强单调性条件下导出后验下界，结合单侧Lipschitz上界，仅依赖网络近似、ODE残差和局部常数。
result: 得到可计算的双向误差证书，线性系统下用特征值显式表达，并解决了硬约束初值导致标量下界无信息的问题。
conclusion: 提出了适用于ODE PINN的严格且可实践的两侧误差证书框架，支持后验诊断与训练正则化。
---

## 摘要
基于物理信息的神经网络（PINNs）结合机器学习与物理定律来求解微分方程。现有结果为PINN预测误差提供了严格的后验上界，但完整的认证还需要互补的下界信息，以获得可计算的双边误差包络。本文在适当的认证状态空间域上，在局部强单调性条件下推导了常微分方程PINN误差的可计算后验下界。我们将这些估计与单侧Lipschitz条件下的互补局部上界相结合，该条件比先前工作中使用的全局Lipschitz假设更弱，并且可以产生更尖锐的上误差带。所得边界仅依赖于神经网络近似、ODE残差以及局部单调性和增长常数，因此无需访问精确解。对于线性时不变和时变系统，我们进一步根据系统矩阵对称部分的最小和最大特征值得出显式公式。我们还讨论了PINN中初始条件的软强制与硬强制之间的区别，并解释了为何精确强制可能使标量下界证书失去信息性。为了在线性情况下恢复有意义的非平凡下界信息，我们使用了基于坐标单位向量的带符号残差有限探针证书。我们还提出了一种证书引导的训练策略，其中传播的上界证书用作辅助正则化器，而下界证书仍作为训练后的诊断工具。总体而言，所提出的框架为ODE的PINN逼近提供了严格且实际可计算的误差证书，同时明确了可以验证这些假设的领域和模型类别。

## Abstract
Physics-informed neural networks (PINNs) combine machine learning with physical laws to solve differential equations. While existing results provide rigorous \emph{a posteriori} upper bounds for PINN prediction errors, complete certification also requires complementary lower information in order to obtain computable two-sided error enclosures. In this paper, we derive computable \emph{a posteriori} lower bounds for PINN errors in ordinary differential equations on suitable certified state-space domains under a localized strong monotonicity condition. We combine these estimates with complementary localized upper bounds under a one-sided Lipschitz condition, which is weaker than the global Lipschitz assumption used in previous work and can yield sharper upper error bands. The resulting bounds depend only on the neural-network approximation, the ODE residual, and local monotonicity and growth constants, and therefore do not require access to the exact solution. For linear time-invariant and time-varying systems, we further derive explicit formulas in terms of the minimal and maximal eigenvalues of the symmetric part of the system matrix. We also discuss the distinction between soft and hard enforcement of initial conditions in PINNs and explain why exact enforcement can make the scalar lower certificate uninformative. To recover nontrivial lower information in the linear setting, we use a signed-residual finite-probe certificate based on coordinate unit vectors. We also formulate a certificate-informed training strategy in which the propagated upper certificate is used as an auxiliary regularizer, while lower certificates remain post-training diagnostics. Altogether, the proposed framework provides rigorous and practically computable error certificates for PINN approximations of ODEs, while making explicit the domains and model classes for which the assumptions can be verified.

---

## 论文详细总结（自动生成）

# 论文中文详细总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：物理信息神经网络（PINNs）求解微分方程时，现有后验误差估计仅提供上界（如 Hillebrecht & Unger 2022），缺少下界信息，无法形成完整的误差包络。完整认证需要同时知道误差可能的最小和最大范围，从而提供可靠的预测质量保证。
- **背景**：PINNs 在安全关键应用中需要可靠性保证。已有统计不确定性量化、双重加权残差等方法难以提供确定性严格证书。作者针对**常微分方程（ODE）**，在**局部强单调性**条件下首次给出可计算的后验误差下界，并与**单侧 Lipschitz** 条件下的上界结合，形成双向误差证书。
- **整体意义**：提出了一个严格且实用的框架，使 PINN 对 ODE 的解具有可计算的两侧误差包络，填补了下界缺失的空白，并可用于训练后诊断和辅助训练正则化。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：在认证的凸状态空间域 \(D\) 上，假设向量场满足局部单侧增长条件，利用能量法推导误差范数的微分不等式，从而得到下界和上界的积分表达式。
- **关键技术细节**：
  - **局部强单调性条件**：\((f(t,u)-f(t,v))\cdot(u-v) \ge \ell_D \|u-v\|^2\)，用于下界。
  - **局部上单侧 Lipschitz 条件**：\((f(t,u)-f(t,v))\cdot(u-v) \le \mu_D \|u-v\|^2\)，用于上界（比全局 Lipschitz 更弱，可给出更紧上界）。
  - **下界公式**（定理1）：  
    \(\|e(t)\| \ge e^{\ell_D t}\|e(0)\| - \int_0^t e^{\ell_D(t-s)}\delta(s)\,ds\)，其中 \(\delta(t)\) 是残差上界。
  - **上界公式**（定理3）：  
    \(\|e(t)\| \le e^{\mu_D t}\|e(0)\| + \int_0^t e^{\mu_D(t-s)}\delta(s)\,ds\)。
  - **线性系统显式化**：对于线性时不变系统，\(\ell_D = m(A) = \lambda_{\min}(\text{sym}A)\)，\(\mu_D = M(A) = \lambda_{\max}(\text{sym}A)\)，得到类似公式（定理5）。对于线性时变系统，采用积分指数形式（推论1）。
  - **硬约束初值下下界恢复**：当 \(e(0)=0\) 时，标量下界变为0。在线性情况下，使用**带符号残差有限探针证书**（定理2）：\(\|e(t)\| \ge \max_{p\in P} \left| p^\top e^{At}e(0) - \int_0^t p^\top e^{A(t-s)}R(s)\,ds \right|\)，其中 \(P\) 为坐标单位向量集。
  - **数值后处理**：采用经典 RK4 方法计算卷积项，并给出误差余项显式上界（定理6），确保计算严格性。
  - **证书引导训练**（第5节）：将传播的上界作为辅助正则化器加入损失函数，通过辅助标量 ODE 高效计算，下界仅用作训练后诊断。

## 3. 实验设计：数据集/场景、基准、对比方法

- **实验场景**：
  1. **非线性径向增长 + 旋转系统**（二维，有解析解）：验证局部证书的有效性，对比全局 Lipschitz 上界和局部段诊断曲线。
  2. **高维刚性线性 ODE**（96维对角系统，刚度比 \(10^4\)）：展示线性常数的显式表示，标量下界失效时有限探针下界的有效性，对比单侧 Lipschitz 上界（\(M(A)=-1\)）和 Lipschitz 上界（\(\|A\|_2=10^4\)）。
  3. **不稳定振荡系统**（二维，带强制项，硬约束初值）：验证证书引导训练的效果，对比三种分支：普通 PINN 延续、基于单侧上界的正则化训练、基于 Lipschitz 上界的正则化训练。
- **基准**：使用解析解或高精度数值解作为真实误差。
- **对比方法**：全局 Lipschitz 上界、局部段诊断曲线（仅用于诊断，非黑箱）、普通 PINN 训练。

## 4. 资源与算力

- **文中未明确说明** GPU 型号、数量、训练时长或计算资源。
- 仅在实验中提及训练阶段（阶段一标准 PINN，阶段二证书引导训练）及网格划分，但未提供硬件配置或具体算力消耗。
- 因此**无法评估**训练所需的计算开销。

## 5. 实验数量与充分性

- **实验数量**：共 3 组独立实验，每组包含若干子图（7 张图、1 张表）。
- **充分性**：
  - 覆盖非线性、高维线性、训练引导三种典型情形，证明方法在不同设定下的有效性。
  - 每个实验都展示了真实误差与证书的对比，并给出了数值结果（如最终误差值、证书值）。
  - **不足**：未与现有其他 PINN 误差估计方法（如统计 UQ、DWR、目标导向估计）进行定量比较；未在 P DE 问题上验证（论文明确限定 ODE）；未进行大量消融实验（如不同常数选择、不同网格大小）。
  - **客观性与公平性**：使用公开解析解或数值解作为基准，结果客观；对比方法（如 Lipschitz 上界）是标准的，公平。

## 6. 论文的主要结论与发现

- 建立了针对 ODE PINN 的**可计算、严格的双侧后验误差证书**（下界 + 上界）。
- 下界在强单调性条件（\(\ell_D > 0\)）下有效；上界在单侧 Lipschitz 条件下比全局 Lipschitz 条件更紧。
- 对于线性系统，证书常数可显式表示为矩阵对称部分的最小/最大特征值。
- 硬约束初值时标量下界可能无效，但通过**带符号残差有限探针**（坐标向量）可恢复有信息的下界。
- 传播的上界证书可作为训练正则化项，有效降低真实误差（以不稳定振荡系统为例，单侧上界训练后误差比普通训练低近一个数量级，比 Lipschitz 训练低两个数量级）。
- 证书宽度可量化认证的紧度，较窄的带表示高可靠性。

## 7. 优点

- **理论创新**：首次为 PINN 误差提供后验下界，并与更优的上界结合，形成完整证书。
- **假设更弱**：上界使用单侧 Lipschitz 条件（比全局 Lipschitz 弱），适用于非自伴/非正规矩阵，证书更紧。
- **线性系统显式化**：公式简洁，可直接计算。
- **实用性强**：证书仅依赖网络输出、残差和局部常数，无需精确解；后处理采用 RK4 并给出严格余项上界。
- **处理硬约束**：通过有限探针技巧解决了标量下界无信息的问题。
- **训练融合**：将上界作为正则化项，兼顾训练与认证。

## 8. 不足与局限

- **标量下界的适用性受限**：当 \(\ell_D \le 0\) 或初始误差为零时，标量下界可能无信息；仅在线性情况下提供有限探针修复，非线性硬约束下界修复未解决。
- **需要局部单调性假设**：不适用于保守系统、哈密顿系统、经典 Lotka-Volterra 模型等非单调相互作用。
- **常数估计保守**：局部常数、残差上界、RK4 余项上界的保守性会导致证书带变宽。
- **实验覆盖不足**：仅含 ODE 案例，未扩展到 PDE；未与多种现有方法系统性对比；未大量消融分析不同超参数影响。
- **算力未报告**：无法评估计算成本的可扩展性。
- **仅适用于认证域内**：若 PINN 或真实解离开认证域，证书失效；需要先验验证域内性。

（完）
