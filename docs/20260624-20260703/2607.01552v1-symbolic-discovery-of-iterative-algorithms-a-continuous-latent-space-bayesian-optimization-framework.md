---
title: "Symbolic Discovery of Iterative Algorithms: A Continuous Latent Space Bayesian Optimization Framework"
title_zh: 迭代算法的符号发现：连续潜空间贝叶斯优化框架
authors: "Tongjia Liu, Ilias Mitrai"
date: 2026-07-02
pdf: "https://arxiv.org/pdf/2607.01552v1"
tags: ["query:sr"]
score: 9.0
evidence: 用符号形式发现迭代优化算法
tldr: 本文针对迭代优化算法的自动发现任务，提出一种连续潜空间贝叶斯优化框架。该框架先利用变分自编码器将离散的更新函数空间连续化，再将算法搜索转化为连续优化问题并使用贝叶斯优化求解。在两个案例中，该方法成功发现符号形式的新更新函数，且计算时间低于传统数学规划方法。该工作为无先验假设的自动算法发现提供了新途径。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.01552v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1782, \"height\": 649, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.01552v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 908, \"height\": 545, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.01552v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1891, \"height\": 556, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.01552v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 913, \"height\": 544, \"label\": \"Figure\"}]"
motivation: 现有自动算法发现方法依赖数学规划，计算成本高且对函数形式有假设，本文旨在高效发现符号形式的新迭代优化算法。
method: 使用变分自编码器学习离散更新函数空间的连续表示，然后通过贝叶斯优化在连续空间中搜索最优更新函数。
result: 在两个案例中，方法成功发现符号形式的更新函数，计算时间低于现有数学规划方法。
conclusion: 提出的框架无需对更新函数形式做假设，能够自动化发现高效的迭代优化算法。
---

## 摘要
在本文中，我们考虑迭代优化算法的自动发现。我们将算法发现任务建模为离散优化问题，并使用潜空间贝叶斯优化搜索新的更新函数。所提出的框架首先利用变分自编码器学习更新函数的离散空间的连续表示，将算法发现任务从离散搜索问题转变为连续搜索问题。随后，利用该连续表示通过贝叶斯优化搜索新算法。应用于两个案例研究表明，所提出的方法能够以符号形式发现新的更新函数，而无需对更新函数的函数形式做任何假设。此外，发现新更新函数所需的计算时间低于现有的基于数学规划的方法。

## Abstract
In this paper, we consider the automated discovery of iterative optimization algorithms. We formulate the algorithm discovery task as a discrete optimization problem and search for new update functions using latent space Bayesian Optimization. The proposed framework first learns a continuous representation of the discrete space of update functions using variational autoencoders, transforming the algorithm discovery task from a discrete to a continuous search problem. The continuous representation is subsequently used to search for new algorithms using Bayesian optimization. Application to two case studies shows that the proposed approach can discover new update functions in symbolic form without any assumptions on the functional form of the update function. Moreover, the computational time required to discover the new update functions is lower than existing mathematical programming-based approaches.

---

## 论文详细总结（自动生成）

### 论文详细总结

#### 1. 核心问题与整体含义（研究动机与背景）
- **问题**：迭代优化算法（如梯度下降）的更新函数通常依赖领域专家手动设计，耗时且主观。现有自动发现方法存在权衡：神经网络参数化导致黑箱、不可解释；基于表达式树和遗传编程的搜索随机且计算低效；混合整数规划方法虽可解释但有全局优化计算成本高、难以处理非封闭性能指标（如求解时间）。
- **目标**：提出一种**高效、可解释的自动算法发现框架**，能够以符号形式发现新的迭代更新函数，无需对函数形式做先验假设。

#### 2. 方法论
- **核心思想**：利用变分自编码器（VAE）将离散的表达式树空间映射到连续潜空间，然后在该连续空间上应用贝叶斯优化（BO）搜索最优更新函数。
- **关键技术细节**：
  - **表示与搜索空间**：更新函数表示为深度为2的完美二叉树（7个节点），每个节点可赋值为二元操作符（+,-,*）、一元操作符（sin,cos,exp）或操作数（变量x_k、梯度∂F/∂x_k、常数cst）。使用二进制变量y_{on}编码，并通过整数化得到输入向量ŷ。
  - **VAE训练**：生成5000个随机有效表达式树，取4000训练。编码器输出潜变量参数（μ,σ²），潜空间维度通过敏感性分析确定为7（因为更高维度不显著改善损失）。训练使用Adam优化器，学习率1e-2，500迭代，总耗时约1.66秒。损失函数为重构交叉熵 + β × KL散度（β控制平衡）。
  - **算法评估**：对于给定树结构，需优化常数c以最小化性能指标——在5次迭代后梯度的L1范数，采用两个目标函数×两个起始点取平均。求解该非线性规划问题使用SCIP（全局求解器），常数值界[−10,10]。若问题不可解（算法不收敛），则赋予大常数惩罚。
  - **BO搜索**：初始随机采样若干有效树，用高斯过程（Matern核）代理性能函数，采集函数为log期望改进。每次BO迭代：在潜空间优化采集函数得到候选z，通过解码器得到树ŷ，若树有效则计算性能m(ŷ)，否则直接惩罚。更新GP后继续迭代。

#### 3. 实验设计
- **案例1（非凸函数）**：
  - 函数：F1=(x₁-2)²+2x₂²-x₁x₂，F2=(x₁-2)⁴+2(x₁-2x₂)²。
  - 每函数2个起始点（固定随机种子1234），最大迭代5次。
  - BO初始10个随机树，21次迭代（共31次评估）。
  - 基准：最速梯度下降（GD，步长调优至最优）。
  - 对比方法：MINLP方法（用BARON和SCIP求解，设定3600秒上限）。
- **案例2（线性回归）**：
  - 函数：min 0.5||Ax-b||²，随机生成两个系统（条件数分别为16.87和7.88）。
  - 同案例1的评估设置（2函数×2起点，5迭代）。
  - BO初始20个随机树，25次迭代（共45次评估）。
  - 基准：最速梯度下降。
- **未进行消融实验**（如不同VAE架构、不同采集函数、不同初始样本数等）。

#### 4. 资源与算力
- **未提及GPU**：VAE训练和BO均在CPU上运行（作者未声明使用GPU）。
- **训练时间**：VAE训练约1.66秒，未计入BO总时间。
- **BO总耗时**：案例1共121秒（评估112秒 + 选择9秒）；案例2共176秒（评估157秒 + 选择18秒）。MINLP对比：BARON在3600秒内未收敛到更好解，SCIP在3600秒内找到与GD相近解（目标值2.8）。

#### 5. 实验数量与充分性
- **实验数量少**：仅两个案例，每个案例仅两个目标函数和两个起始点，BO迭代次数有限（31/45次评估）。
- **充分性不足**：
  - 未在更高维问题、更多目标函数、不同任务（如方程组求解）上验证。
  - 未与遗传编程、强化学习等现有方法直接对比性能或计算时间。
  - 缺失统计性分析（如重复试验、误差条）。
  - 性能指标单一（仅最后梯度范数），未考虑收敛速度、鲁棒性等。
  - 未研究VAE重建误差对搜索的影响，未评估潜空间覆盖范围。
- **客观性**：结果展示清晰，但结论的泛化能力受限于实验规模。

#### 6. 主要结论与发现
- 成功发现符号形式的更新函数：
  - **案例1**：发现状态依赖步长更新 `x_{i+1} = x_i - 0.035(2.18 + x_i)∇F(x_i)`，其性能优于调优后的GD（5次迭代后梯度范数更低），且能以更低计算时间（121秒）找到比MINLP方法（3600秒内未收敛）更好的解。
  - **案例2**：发现带小正则项的更新 `x_{i+1} = x_i - 0.11∇F(x_i) - 1.61×10^{-3} x_i`，对条件数较大的矩阵表现优于GD。
- 提出框架的**计算效率显著高于数学规划方法**，且能够“重新发现”经典算法（如GD），说明VAE学到了有意义的连续表示。

#### 7. 优点
- **可解释性**：输出为符号形式的表达式树，便于分析收敛性。
- **无先验假设**：不对更新函数的形式做任何限制（仅受树深度和操作符集影响）。
- **计算效率**：利用BO的样本高效特性，结合VAE连续化，避免昂贵的离散全局优化。
- **通用性**：框架可扩展至其他迭代算法发现任务（如方程组求解、PDE求解）。
- **预处理成本低**：VAE训练快速（<2秒），BO搜索耗时远低于数学规划基准。

#### 8. 不足与局限
- **实验规模小**：仅两个案例，每个案例函数数量少，结果可能不具普适性。
- **缺乏对比**：未与遗传编程、大型语言模型引导搜索等主流符号发现方法比较。
- **性能指标单一**：仅用5次迭代后的梯度范数，未考虑真实收敛时间、稳定性等。
- **潜在偏差风险**：目标函数和起始点生成依赖固定随机种子，未进行多种子实验；BO初始树随机生成，可能引入偏差。
- **应用限制**：
  - 依赖VAE重建质量，潜空间可能漏掉某些有效树；无效树直接惩罚可能丢失好候选。
  - 树深度固定为2，复杂更新函数需更大深度，可能带来搜索难度和计算开销增长。
  - 常数优化使用了全局求解器（SCIP），对于更大规模问题评估成本可能急剧上升。
- **泛化性未验证**：未在高维优化、非凸强非线性、工程实际问题上测试。

（完）
