---
title: Learning Parametric Nitrogen Fertilizer Response Curves Using Neuro Symbolic Regression
title_zh: 使用神经符号回归学习参数化氮肥响应曲线
authors: "Giorgio Morales, John Sheppard"
date: 2026-05-29
pdf: "https://arxiv.org/pdf/2605.31276v1"
tags: ["query:sr-piml"]
score: 9.0
evidence: 使用神经符号回归进行参数化曲线发现
tldr: 精准农业中氮肥响应曲线建模面临预设参数形式或黑箱模型的局限，难以发现地点特异性函数关系。本文提出神经符号回归方法，基于Transformer多组符号骨架预测策略，自动发现跨管理区的共享功能结构，并用遗传算法拟合数据。在合成和真实冬小麦数据上，该方法无需预设函数形式即能恢复正确表达式，拟合误差低于传统二次平台和指数模型，且能捕捉不同空间区域的多样性行为。该工作为发现地点特异性农学关系提供新工具，支持精准农业决策。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.31276v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 707, \"height\": 393}, {\"url\": \"assets/figures/arxiv/2605.31276v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 842, \"height\": 267}, {\"url\": \"assets/figures/arxiv/2605.31276v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 838, \"height\": 959}, {\"url\": \"assets/figures/arxiv/2605.31276v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 854, \"height\": 415}, {\"url\": \"assets/figures/arxiv/2605.31276v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 923, \"height\": 697}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.31276v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 930, \"height\": 650}, {\"url\": \"assets/tables/arxiv/2605.31276v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 916, \"height\": 497}, {\"url\": \"assets/tables/arxiv/2605.31276v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 776, \"height\": 385}, {\"url\": \"assets/tables/arxiv/2605.31276v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 660, \"height\": 329}]"
motivation: 现有氮肥响应曲线建模无法在无需预设函数形式下发现地点特异性功能关系。
method: 采用神经符号回归，基于Transformer多组符号骨架预测策略，构建多样输入子集并强制一致性，再用遗传算法拟合参数。
result: 在合成数据上实现数据稀缺下的正确表达式恢复；在真实冬小麦数据上，不同管理区曲线拟合误差低于传统二次平台和指数模型。
conclusion: 神经符号回归能有效发现地点特异性农学关系，支持精准农业决策。
---

## 摘要
精确模拟作物对氮肥（N）的响应是精准农业中的一个基本挑战，因为它同时影响经济效益和环境可持续性。现有方法要么依赖预定义的参数形式，要么依赖不透明的机器学习模型，限制了其从数据中解释或发现特定地点功能关系的能力。在这项工作中，我们提出了一种神经符号回归（SR）方法，无需假设预定义的函数形式即可学习参数化的N响应曲线。我们的方法集成了基于Transformer的多集符号骨架预测策略，能够发现跨多个子域或管理区（MZ）的共享功能结构。通过构建多样的输入子集并强制执行它们之间的一致性，该方法恢复了鲁棒的符号骨架，随后使用遗传算法将其拟合到观测数据上。该框架首先在合成的一维问题上进行评估，以测试其在不同程度的认知不确定性下的鲁棒性。结果表明，即使在数据稀缺的情况下，所提出的SR方法也能恢复正确的表达式。在本工作中，我们展示了将我们的方法应用于真实冬小麦数据的结果，为田块内不同管理区学习不同的参数化N响应曲线。结果表明，发现的表达式不仅比传统模型（如二次平台和指数函数）实现了更低的拟合误差，而且还捕捉了跨空间区域的不同功能行为。这证明了神经符号回归在发现特定地点的农艺关系和支持精准农业中知情决策方面的潜力。

## Abstract
Accurately modeling crop response to Nitrogen (N) fertilization is a fundamental challenge in precision agriculture, as it impacts both economic returns and environmental sustainability. Existing approaches either rely on predefined parametric forms or opaque machine learning models, limiting their ability to interpret or discover site-specific functional relationships from data. In this work, we propose a neuro symbolic regression (SR) approach to learn parametric N-response curves without assuming a predefined functional form. Our approach integrates a transformer-based Multi-Set Symbolic Skeleton Prediction strategy, enabling the discovery of shared functional structures across multiple subdomains or management zones (MZs). By constructing diverse input subsets and enforcing consistency across them, the method recovers robust symbolic skeletons that are subsequently fitted to observed data using a genetic algorithm. This framework was first evaluated on synthetic one-dimensional problems to assess its robustness under varying levels of epistemic uncertainty. The results demonstrate the ability of the proposed SR approach to recover correct expressions even in data-scarce regimes. In this work, we present the results of applying our method to real-world winter wheat data, learning distinct parametric N-response curves for different MZs within a field. The results show that the discovered expressions not only achieve lower fitting errors than traditional models such as quadratic-plateau and exponential functions, but also capture diverse functional behaviors across spatial regions. This demonstrates the potential that neuro SR has to enable the discovery of site-specific agronomic relationships and support informed decision-making in precision agriculture.

---

## 论文详细总结（自动生成）

### 论文总结：Learning Parametric Nitrogen Fertilizer Response Curves Using Neuro Symbolic Regression

#### 1. 核心问题与整体含义（研究动机和背景）
- **问题**：精准农业中，准确建模作物对氮肥（N）的响应曲线对于优化施肥、提高经济效益和减少环境影响至关重要。然而，现有方法要么依赖预定义的参数形式（如二次-平台模型、指数模型），要么使用不透明的机器学习模型，导致无法从数据中发现**地点特异性**的功能关系。
- **背景**：管理区（MZ）划分技术可将田块划分为具有相似响应特性的子区域，但缺乏一种无需预设形式、能自动发现各区域内在数学表达式的方法。符号回归（SR）有望实现这一目标，但面临1-D曲线因数据稀疏导致的认知不确定性挑战。
- **整体含义**：本文提出一种**神经符号回归（Neuro SR）**方法，结合Transformer多集符号骨架预测与遗传算法，从神经网络生成的N响应曲线中自动提取参数化表达式，**无需事先指定函数形式**，从而支持可解释的精准农业决策。

#### 2. 方法论
- **核心思想**：利用**多集符号骨架预测（MSSP）**策略，从神经网络预测曲线中恢复共享的符号骨架（即函数形式，其中常数由占位符表示），再通过遗传算法拟合具体参数。
- **关键技术细节**：
  - **1-D问题处理**：将训练好的模型在输入域上预测得到完整曲线，然后构造多组**子集**（随机选择不同范围的子区间），每个子集都出自同一条曲线，假设它们共享相同的骨架。多集Transformer（基于SeTGAP框架）从这些子集中预测一个共同的符号骨架。
  - **算法流程（Algorithm 1）**：
    1. 对每条曲线，从输入域随机截取80%~100%的子区间，得到 n 个点，构造 NS 个子集集合 D。
    2. 用预训练的多集Transformer g 对 D 预测候选骨架（beam size nB=3）。
    3. 重复 ncand=5 次，获得候选骨架列表，去重。
    4. 用遗传算法 fitCoefficients 将各骨架拟合到观测数据 D，选择MSE最低的表达式作为最终结果。
  - **MZ曲线学习（Algorithm 2）**：对于每个管理区，随机选择 NS 条不同的N响应曲线（每条曲线对应一个田点），对每条曲线随机采样一个子集构建多集，然后运行Algorithm 1的变体得到管理区级别的共享骨架。同样重复 ncand 次，在测试曲线上验证拟合误差。
  - **数学表示**：最终得到形如 `c1 + c2 * tanh(c3 + c4 * x)` 或 `c1 + c2 * exp(c3 * sin(c4 + c5 * x))` 等表达式，其中 `c_i` 为待拟合参数。

#### 3. 实验设计
- **合成数据实验**：
  - 数据集：三个带异方差噪声的一维函数：`cos`（形式类似 `sin(x²)`）、`hetero`（类似 `sin(x)`）、`cosqr`（类似 `cos(x²)`）。初始数据集稀疏，模拟高认知不确定性。
  - Benchmark：采用自适应采样框架ASPINN（生成预测区间的神经网络），每轮增加5个采样点，逐步降低不确定性。
  - 对比方法：无外部对比，主要评估符号回归随迭代次数收敛到真实函数形式的能力（恢复功能形式的正确性及MSE）。
- **真实冬小麦数据实验**：
  - 数据：一块冬小麦田（Field A），包含预训练的2D-3D卷积神经网络生成的N响应曲线（基于[13]）。田块被划分为4个管理区（MZ1~MZ4）。
  - Benchmark：传统方法——**二次-平台模型**（quadratic-plateau）和**指数模型**（exponential）。
  - 对比指标：每个管理区内所有田点拟合曲线的**平均绝对误差**（`¯r`，即拟合值与原NN曲线之差的绝对值平均）。
- **结果概要**：
  - 合成数据：在迭代至20轮左右时，符号表达式稳定匹配真实函数形式（表I）；早期可能因数据稀疏而走偏，但多集策略帮助快速收敛（图3）。
  - 真实数据：SeTGAP为每个MZ发现了不同形式的表达式，且平均误差均低于两种传统模型（表II）。例如MZ1和MZ2的骨架为 `c1 + c2 * tanh(c3 + c4*x)`，MZ3为线性项加余弦项，MZ4为指数嵌套正弦。

#### 4. 资源与算力
- 论文**未明确说明**使用的GPU型号、数量、训练时长。仅提到Multi-Set Transformer是预训练的，且“SeTGAP”框架已有先前工作；实验运行在常规计算资源上。合成实验每次AS迭代运行一次算法1，ncand=5，beam size=3；真实数据每个MZ运行一次算法2，ncand=5。算力需求相对较小，但未提供量化数据。

#### 5. 实验数量与充分性
- **合成实验**：3个函数，每个函数50次AS迭代（每轮5个新样本），报告了8个代表性迭代的表达式和MSE（表I），并展示了其中一个函数（cosqr）的预测曲线演化（图3）。实验能证明多集策略在不确定性下的收敛性，但未进行重复性统计（如多次独立运行分析）。
- **真实实验**：单田块（Field A），4个管理区。每个管理区ncand=5，选最优骨架。与两种传统方法对比，给出平均误差（表II）。**缺乏**以下实验：
  - 消融研究（如不同NS、ncand、子集大小的影响）。
  - 跨多个田块/作物/年份的泛化性测试。
  - 与其他符号回归方法（如Deep Symbolic Regression、Eureqa）的对比。
  - 对骨架形式鲁棒性的敏感性分析。
- **总体评价**：实验是概念验证（proof-of-concept），充分性不足；但设计清晰、结果合理，能支撑主要结论，即神经符号回归可以自动发现地点特异性N响应表达式。

#### 6. 主要结论与发现
- 提出的多集符号骨架预测策略能有效从1-D曲线（即使存在认知不确定性）中恢复正确的函数形式，且随着数据增加，估计表达式稳定收敛。
- 在真实冬小麦N响应曲线学习中，为不同管理区自动发现的表达式具有不同的结构（如tanh、线性+余弦、指数+正弦），且拟合误差均低于传统二次-平台和指数模型，表明**地点特异性建模的优越性**。
- 该方法提供了可解释的数学模型，有助于农学家理解不同区域对N肥的响应行为差异（如斜率、饱和平台等），从而支持精准施肥决策。

#### 7. 优点
- **无需预设函数形式**：完全从数据中自动发现符号骨架，避免了人工先验的偏差。
- **鲁棒性强**：多集策略通过采样不同子区间并强制共享骨架，减轻了局部数据稀疏和噪声导致的过拟合风险。
- **可解释性**：最终表达式简洁（通常含3~5个参数），可直接用于计算经济最优氮肥率（EONR），支持透明决策。
- **方向新颖**：将神经符号回归引入精准农业，为从黑箱模型中提取可解释知识提供了新范式。

#### 8. 不足与局限
- **实验覆盖不足**：仅在一个田块上验证，且未与现有符号回归方法（如DSR、Eureqa）进行系统比较，通用性和优势尚不明确。
- **方法限制**：当前仅针对1-D曲线设计（因MZ抽象后每个区域变为单变量），能否扩展到多变量SR、或对高维协变量同时建模未讨论。
- **依赖预训练模型**：Multi-Set Transformer需在大规模符号骨架数据集上预训练，其泛化能力受限于训练分布。
- **不确定性处理简化**：合成实验的异方差噪声设置简单，真实场景的噪声可能更复杂（如空间相关、非平稳性），方法在真实噪声下的表现未测试。
- **缺少消融分析**：未量化各模块（如不同NS、子集选择策略、ncand）的贡献，也未分析骨架候选重复率等统计信息。
- **资源与实现细节缺失**：未报告运行时间和计算开销，对于实际应用部署的参考价值有限。

（完）
