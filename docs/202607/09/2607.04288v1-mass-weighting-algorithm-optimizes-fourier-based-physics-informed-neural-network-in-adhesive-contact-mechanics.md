---
title: Mass weighting algorithm optimizes Fourier-based physics-informed neural network in adhesive contact mechanics
title_zh: 质量加权算法优化基于傅里叶的物理信息神经网络在粘附接触力学中的应用
authors: "Yunong Zhou, Kaifeng Huang, Chaofan Du, Yang Xu, Hengxu Song"
date: 2026-07-05
pdf: "https://arxiv.org/pdf/2607.04288v1"
tags: ["query:physics-ml"]
score: 8.0
evidence: 物理信息神经网络用于求解偏微分方程，采用频谱预处理
tldr: 弹性接触力学中傅里叶基PINN因频谱刚度不平衡而收敛缓慢，短波模式主导梯度。本文提出质量加权谱预处理策略，在傅里叶空间重加权位移梯度，放大低波数并滤波抑制噪声。在粘着线接触基准中，400次Adam迭代达到机器零残差，位移和应力场与GFMD解一致。方法无需显式格林函数积分，适用于均匀网格，可自然推广至二维粗糙表面。
source: arxiv
selection_source: carryover_cache
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.04288v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1155, \"height\": 388, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.04288v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1137, \"height\": 770, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.04288v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1131, \"height\": 816, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.04288v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1134, \"height\": 772, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.04288v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1133, \"height\": 732, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.04288v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1133, \"height\": 1126, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.04288v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1162, \"height\": 964, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.04288v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1306, \"height\": 416, \"label\": \"Table\"}]"
motivation: 傅里叶基PINN在弹性接触中因频谱刚度不平衡，短波模式主导梯度更新导致收敛停滞。
method: 在傅里叶空间用质量加权函数重加权位移梯度，放大低波数分量并内置低通滤波抑制亚网格噪声。
result: 粘着线接触基准中400次Adam迭代达到机器零残差，位移和应力场与GFMD解定量一致。
conclusion: 方法无需格林函数积分，直接在均匀网格上最小化能量，可扩展至二维粗糙表面问题。
---

## 摘要
用于弹性接触力学的物理信息神经网络(PINNs)存在谱刚度不平衡问题，即弹性核随波数线性增长，导致短波长模式主导梯度更新并阻碍宏观变形的收敛。我们提出了一种谱预处理策略，在反向传播前对傅里叶空间中的位移梯度进行重新加权，通过质量加权(MW)函数放大低波数分量，同时通过内置低通滤波器抑制亚网格噪声。应用于粘附线接触问题，对于指定的基准算例，质量加权PINN在400次Adam迭代内达到机器零残差损失，而参考基准的损失停滞在高三个数量级。收敛的位移和接触应力场与格林函数分子动力学(GFMD)解在定量上一致，涵盖从拉伸到压缩的平滑赫兹接触以及粗糙度跨越多个波长量级的粗糙表面。该方法直接在均匀实空间网格上操作，无需显式格林函数积分或求积规则，完全基于最小化标量能量函数来表述。扩展到二维粗糙表面是直接的，因为傅里叶弹性能和谱预处理子都仅依赖于波数大小。

## Abstract
Physics-informed neural networks (PINNs) for elastic contact mechanics suffer from a spectral stiffness imbalance,that is, the elastic kernel grows linearly with wave number, causing short-wavelength modes to dominate gradient updates and stall convergence of the macroscopic deformation. We introduce a spectral preconditioning strategy that reweights displacement gradients in Fourier space before back-propagation, amplifying low wavenumber components through a mass weighting (MW) function while suppressing sub-grid noise via a built-in low-pass filter. Applied to adhesive line contact problems, the mass weighted PINN reaches machine-zero residual loss within 400 Adam iterations for specified benchmark, whereas the reference benchmark stalls at three orders of magnitude higher loss. The converged displacement and contact stress fields agree quantitatively with Green's function molecular dynamics (GFMD) solutions for both smooth Hertz contact at pressures spanning tension to compression and rough surfaces with roughness covering several decades of wavelength. The method operates directly on a uniform real-space grid, requires no explicit Green's function integration or quadrature rules, and is formulated entirely in terms of minimising a scalar energy function. Extension to two-dimensional rough surfaces is direct, as both the Fourier elastic energy and the spectral preconditioner depend only on the wave-number magnitude.

---

## 论文详细总结（自动生成）

好的，作为一名资深学术论文分析助手，我将根据您提供的论文内容，对《Mass weighting algorithm optimizes Fourier-based physics-informed neural network in adhesive contact mechanics》一文进行结构化、深入且客观的总结。

---

### 论文总结：质量加权算法优化基于傅里叶的物理信息神经网络在粘附接触力学中的应用

#### 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：传统用于求解弹性接触力学的物理信息神经网络(PINNs)面临严重的**频谱刚度不平衡**问题。其物理约束（弹性能）在傅里叶空间中，刚度核 `qE*/2` 随着波数 `q` 线性增长。这导致短波长（高频）的变形模式主导了梯度更新，而长波长（低频、宏观）的变形模式收敛极慢，最终使训练陷入停滞，无法得到准确的宏观变形和接触应力场。
- **整体含义**：本文旨在解决上述频谱不平衡问题，使基于能量最小化原理的PINNs能够高效、准确地求解复杂的粘附接触力学问题（包括光滑赫兹接触和多尺度粗糙表面接触），从而提供一种**无需显式格林函数积分**、可直接在均匀网格上运行的新颖数值框架。

#### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：提出一种**频谱预处理（Spectral Preconditioning）** 策略，在梯度反向传播之前，于傅里叶空间中重新平衡不同波数模式对梯度更新的贡献。具体而言，通过一个**质量加权（Mass Weighting, MW）函数**放大低波数（宏观）模式的梯度信号，同时使用**内置低通滤波器**抑制高波数（亚网格级）的噪声。
- **关键技术细节**：
    1.  **问题公式化**：将接触问题转化为一个**标量能量函数最小化**问题。总势能 Π = 弹性能 U_el + 外功 U_ext + 粘附界面能 U_int。
    2.  **网络架构**：使用**傅里叶特征映射** 对输入坐标 `x` 进行编码，然后通过一个全连接前馈神经网络 `u_θ(x)` 输出位移场。
    3.  **频谱预处理流程**（公式5, 12, 13, 14）：
        - 计算损失函数 Π 对位移场 `u` 的梯度 `∂Π/∂u`。
        - 将该梯度通过傅里叶变换到频域 `ĝ(q)`。
        - 应用一个组合的频谱加权函数 `G(q)`：
            - **质量加权函数 `w(q)`**：`w(q) = ((E*q_max / 2) / sqrt(k0² + (qE*/2)²))^α`。该函数在低 `q` 处放大梯度，在高 `q` 处衰减。
            - **低通滤波器 `f(q)`**：`f(q) = 1 / (1 + (q/q_c)^β)`，用于抑制亚网格尺度的高频噪声。
        - 将加权后的梯度 `G(q) * ĝ(q)` 通过傅里叶逆变换回实空间，得到预处理后的梯度 `δΠ/δu_precond`。
    4.  **训练过程**：使用预处理后的梯度通过自动微分更新网络参数 `θ`。预处理在预热阶段（100-200步）后激活。
- **算法流程**：输入坐标 `x` → 傅里叶特征编码 → 神经网络 → 位移场 `u_θ(x)` → 计算总势能 Π → 计算梯度 `∂Π/∂u` → 傅里叶变换 → 频谱重加权 (`G(q)`) → 逆傅里叶变换 → 预处理后梯度 → 反向传播更新网络参数。

#### 3. 实验设计：数据集、基准与对比方法

- **数据集/场景**：作者构建了两种代表性的接触问题：
    1.  **光滑赫兹接触**：一个刚性抛物线形压头压入弹性半空间。验证了三个名义压力水平：拉伸 (`p0/E* = -0.01`)、零外载 (`0`)、压缩 (`0.01`)。
    2.  **分形粗糙表面接触**：一个具有Hurst指数 `H=0.5` 的随机粗糙表面，其功率谱密度(PSD) `C(q)` 覆盖两个波数量级。同样测试了三个名义压力水平，涵盖了从低拉伸到高压缩的过渡区间。
- **基准 (Benchmark)**：**格林函数分子动力学（GFMD）** 方法。这是大规模粗糙接触模拟的经典标准方法，通过快速傅里叶变换实现 `O(N log N)` 复杂度的求解。
- **对比方法**：主要对比了**无质量加权预处理（Mass Weighting off）** 的傅里叶基PINN。通过对比有无预处理的损失下降曲线、最高频傅里叶模的振幅以及最终的接触应力场来证明方法的有效性。最终的PINN结果也与GFMD参考解进行了定量对比。

#### 4. 资源与算力

- **未明确说明**：论文中未提及使用的具体GPU型号、数量、训练时长或总体训练算力消耗。仅提到在`nx=2048`的空间离散点、`nl`个隐藏层和`nh`个神经元下，经过600次Adam迭代完成训练。这表明总体计算需求相对常规，但具体算力细节缺失。

#### 5. 实验数量与充分性

- **实验数量**：
    - 1组“有/无预处理”的消融实验（图3、4、5），用于展示方法的核心效果。
    - 2种接触类别（赫兹、粗糙），共计6个不同的压力/场景（图6、7）。
- **实验充分性与客观性**：
    - **优点**：实验设计具有代表性，覆盖了从光滑到粗糙、从拉伸到压缩的典型接触场景。通过直接对比GFMD这一公认基准，以及进行严格的定量误差分析（RMSE, NRMSE，表1），结论非常有说服力，客观性很强。
    - **不足**：虽然场景具有代表性，但只测试了**一维线接触**问题和**一种粗糙表面实现**（H=0.5）及**一种粘附模型（Morse势）**。这限制了结论在更广泛、更复杂场景（如二维表面、不同粘附势、不同非线性本构）下的普适性证明。

#### 6. 论文的主要结论与发现

1.  **频谱预处理的必要性**：对于基于能量最小化的PINNs在弹性接触问题中的应用，频谱预处理**不是锦上添花，而是稳定收敛的必要条件**。无预处理时，训练停滞，产生非物理的振荡应力场。
2.  **显著收敛加速**：质量加权预处理使得损失函数在仅400次Adam迭代后便达到“机器零”级别，而无预处理则停滞在高出三个数量级的残差上。
3.  **定量精准预测**：经过预处理的PINN所预测的位移场和接触应力场，与GFMD参考解在**定量上高度一致**。在所有6个测试案例中，位移的归一化均方根误差(NRMSE)均值仅为 `6.5 × 10⁻⁵`，应力的NRMSE均值为 `2.5 × 10⁻⁴`。
4.  **方法的通用性与扩展性**：该方法完全基于最小化能量函数，无需格林函数显式积分，可直接、自然地推广到二维粗糙表面问题。

#### 7. 优点：方法或实验设计上的亮点

- **方法新颖性**：将神经网络中的“频谱偏差”问题与弹性力学中的“频谱刚度”问题联系起来，并针对性地设计了高效的频谱预处理策略，视角独特且巧妙。
- **简洁高效**：公式化过程非常简洁，将一个复杂的粘附接触问题转化为一个标量能量最小化问题，并且预处理操作完全在傅里叶空间内完成，计算高效。
- **实验扎实**：与公认的GFMD基准进行定量对比，并以表格形式提供了所有场景下的数值误差，结果透明、可信度高。
- **通用性潜力**：预处理子与具体的粘附势无关，理论上可推广到其它类型的界面模型（如Lennard-Jones势、率相关摩擦定律）。

#### 8. 不足与局限

- **应用场景局限**：该方法目前仅限于**一维线接触**、**均匀网格**、**半无限大弹性半空间**的假设。对于有限厚度、复杂几何体或各向异性材料等更普适的接触问题，其有效性尚待验证。
- **参数依赖与敏感性**：预处理函数 `G(q)` 中包含多个需要手动设置的超参数（如 `α`, `k₀`, `q_c`, `β`）。论文中声称这些参数无需手动调整，但其在不同问题下的敏感性或自动调参策略未进行讨论。
- **微结构分辨率**：在压力最低、接触斑稀疏的场景（图7，p0列），应力场的NRMSE（1.0×10⁻³）相对其他场景有所增大，表明在处理极端稀疏的微凸体接触时，精度可能有所下降。
- **算力报告缺失**：未提供任何GPU型号、数量或训练时长等信息，这影响了其他研究者对其计算成本的可复现性评估。

（完）
