---
title: "Branch2Skill: Efficient Skill Evolution Through Reasoning Trees"
title_zh: Branch2Skill：通过推理树实现高效技能进化
authors: "Yanwei Ren, Haotian Zhang, Likang Xiao, Jiaxing Huang, Jiayan Qiu, Baosheng Yu, Quan Chen, Liu Liu"
date: 2026-08-09
pdf: "https://arxiv.org/pdf/2608.08677v1"
tags: ["query:cfd-agent"]
score: 9.0
evidence: 通过推理树与蒙特卡洛树搜索实现技能进化
tldr: "现有技能进化方法依赖单条轨迹，早期推理错误会传播并削弱反馈，导致重复 rollout-诊断-更新循环，token 成本高。Branch2Skill 通过蒙特卡洛树搜索在固定预算下生成多样化推理轨迹，对比精英路径与同前缀兄弟路径，提取逐步证据并蒸馏为可复用更新。在六个基准上，相比 SkillOpt 节省 73.2% token 且性能更优。表明推理树不仅能提升轨迹搜索，还能提供更丰富的监督，提高技能进化效率。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.08677v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 865, \"height\": 307, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.08677v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 824, \"height\": 336, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.08677v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1781, \"height\": 704, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.08677v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1778, \"height\": 614, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.08677v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1516, \"height\": 529, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.08677v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1684, \"height\": 423, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.08677v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 722, \"height\": 398, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.08677v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1820, \"height\": 1995, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.08677v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 880, \"height\": 661, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.08677v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 881, \"height\": 587, \"label\": \"Table\"}]"
motivation: 现有方法基于单轨迹，早期错误传播导致反馈弱，技能改进需多次试错，token 开销大。
method: 用蒙特卡洛树搜索生成推理树，对比精英路径与共享前缀的兄弟路径，提取逐步证据并蒸馏为更新。
result: "在六个基准上优于 SkillOpt，token 减少 73.2%，性能更佳。"
conclusion: 推理树可提供更丰富监督，实现高效技能进化。
---

## 摘要
技能进化通过随时间累积的反馈来提升智能体的技能，其中失败的轨迹往往通过揭示不完整或误导性的行为来提供有信息量的信号。然而，现有方法主要依赖单一轨迹，其中早期的推理错误会传播到后续步骤，从而削弱可用于技能改进的反馈。因此，提升技能需要反复进行推演、诊断和更新的循环，从而产生大量的 token 成本。为应对这一挑战，我们提出了 Branch2Skill——一个高效的框架，它将单个推理树转化为用于技能进化的密集监督。对于每个任务或问题，Branch2Skill 在固定预算下执行蒙特卡洛树搜索以获得多样化的推理轨迹，然后将精英路径与共享相同前缀的兄弟替代路径进行比较，以提取关于应保留、修改或避免哪些推理模式的逐步证据。最后，Branch2Skill 将多步证据提炼为可重用的更新，使单个推理树能够为多个推理步骤提供监督，并减少对反复推演-更新循环的需求。在涵盖推理和智能体任务的六个基准测试中，Branch2Skill 持续提升任务性能，同时提高技能进化效率。例如，以 GPT 5.5 作为目标模型时，Branch2Skill 相比 SkillOpt 减少了 73.2% 的 token 使用量，同时实现了更优的性能。这些结果表明，推理树不仅能支持更有效的轨迹搜索，还能为更高效的技能提升提供更丰富的监督。代码将予以发布。

## Abstract
Skill evolution improves agent skills through feedback over time, with failed trajectories often providing informative signals by revealing incomplete or misleading behaviors. However, existing methods mainly rely on single trajectories, where early reasoning errors can propagate through subsequent steps and weaken the feedback available for skill refinement. Consequently, improving skills requires repeated cycles of rollout, diagnosis, and update, incurring substantial token costs. To address this challenge, we introduce Branch2Skill, an efficient framework that transforms a single reasoning tree into dense supervision for skill evolution. For each task or problem, Branch2Skill performs Monte Carlo tree search under a fixed budget to obtain diverse reasoning trajectories, then compares an elite path with sibling alternatives sharing the same prefixes to extract step-wise evidence about which reasoning patterns to retain, revise, or avoid. Finally, Branch2Skill distills multi-step evidence into reusable updates, allowing one reasoning tree to provide supervision across multiple reasoning steps and reducing the need for repeated rollout-update cycles. Across six benchmarks covering reasoning and agentic tasks, Branch2Skill consistently improves task performance while enhancing skill evolution efficiency. For example, with GPT 5.5 as the target model, Branch2Skill uses 73.2% fewer tokens than SkillOpt, while achieving superior performance. These results demonstrate that reasoning trees can support not only more effective trajectory search, but also richer supervision for more efficient skill improvement. Code will be published.

---

## 论文详细总结（自动生成）

好的，以下是对论文《Branch2Skill: Efficient Skill Evolution Through Reasoning Trees》的详细中文总结。

---

## 1. 论文的核心问题与整体含义（研究动机和背景）

论文聚焦于**大语言模型（LLM）智能体的技能进化**问题。

- **背景**：LLM 智能体需要在不断执行任务的过程中积累经验、提升能力。由于直接更新模型参数成本高昂且不切实际，现有研究倾向于将学习过程外部化，通过维护可重用的“技能”（自然语言规则、程序或推理模式）来改进智能体行为。
- **核心挑战**：现有技能进化方法（如 SkillOpt）主要依赖**单一轨迹（single rollout）** 来提取更新信号。然而，单条轨迹中**早期的推理错误会向后传播**，使得后续步骤的反馈被污染或变得模糊。这导致一次试错只能产生少量可靠信号，要获得多个改进信号就必须反复执行“推演-诊断-更新”的循环，**token 开销巨大**。
- **关键洞察**：推理树（reasoning tree）中包含的“同前缀兄弟节点”（sibling nodes）可以为局部决策提供天然的对照实验。通过比较精英路径与共享相同前缀的替代分支，可以隔离出**单个决策步骤的影响**，从而在一个推理树中提取出多个步骤的密集监督信号。
- **论文目标**：提出 **Branch2Skill** 框架，将单一的推理树转化为密集的监督信号，用于高效的技能进化，减少对重复 rollout-update 循环的依赖，显著降低 token 成本。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

### 核心思想
Branch2Skill 的核心在于**“一次搜索，全局监督”**：利用蒙特卡洛树搜索（MCTS）在固定预算下生成覆盖多个决策点的推理树，利用树结构本身的性质（共享前缀的兄弟节点）形成局部对比，从而将一次搜索的结果转化为多个步骤的技能更新证据。

### 技术流程（分步描述）

1. **Stepwise Reasoning Tree Construction（逐步推理树构建）**
   - 对于每个训练问题，构建一棵推理树。根节点为空，每个非根节点代表一个中间推理步骤或环境动作。
   - 采用 **PUCT（Predictor + UCT）** 算法进行节点选择，公式如下：
     \[
     \text{PUCT}(u, v) = Q(v) + c_{\text{puct}} \cdot P(v|u) \cdot \frac{\sqrt{\max(1, N(u))}}{1 + N(v)}
     \]
     其中 \(Q(v)\) 是节点的平均得分（由目标模型对完成该前缀的成功可能性进行自评估），\(N(\cdot)\) 是访问次数，\(P(v|u)\) 是先验概率，\(c_{\text{puct}}\) 控制探索程度。搜索在迭代预算、最大深度等约束下进行，并在搜索结束后对未完成的分支进行补全以获得最终结果。

2. **Shared Prefix Evidence Extraction（共享前缀证据提取）**
   - 搜索结束后，选择一条**精英路径** \(\pi^* = \{S^1_{i^*_1}, \ldots, S^L_{i^*_L}\}\)（优先选择成功的路径，若无成功路径则选择得分最高的失败路径）。
   - 沿着精英路径的每一层，收集与当前选中节点**共享同一父节点**的兄弟节点集合 \(B_d\)。
   - 由于兄弟节点共享相同的推理前缀，它们最终结果的差异可以**局部归因于当前决策点的选择**，由此形成每个步骤的对比证据。

3. **Skill Revision and Validation（技能修订与验证）**
   - 将从多个推理树中收集的证据汇总，发送给**技能模型（优化器）**。优化器对当前技能 \(K_t\) 执行三种操作之一：
     - **APPEND**：添加缺失的规则、条件或前置条件。
     - **REPLACE**：替换不合理或与证据冲突的指导。
     - **DELETE**：删除误导性或冗余的指导。
   - 生成候选技能 \(\hat{K}_{t+1}\) 后，需要在固定的验证集 \(V\) 上进行**直接推理**（不使用树搜索）评估。使用**非回归规则**（non-regression rule）作为门控机制：
     \[
     K_{t+1} = \begin{cases} \hat{K}_{t+1}, & \text{if } g(\hat{K}_{t+1}; V) \succeq g(K_t; V) \\ K_t, & \text{otherwise} \end{cases}
     \]
     只有验证指标不下降的候选技能才会被保留。

## 3. 实验设计：数据集 / 场景、Benchmark、对比方法

### 评估基准（Benchmarks）
论文在六个不同场景的基准上进行了评估，涵盖推理和智能体任务：

| Benchmark | 任务类型 |
| :--- | :--- |
| **SearchQA** | 带噪声证据的问答 |
| **SpreadsheetBench** | 真实的电子表格操作 |
| **OfficeQA** | 企业文档推理 |
| **DocVQA** | 文档图像问答 |
| **LiveMathematicianBench (LiveMath)** | 研究级数学推理 |
| **ALFWorld** | 具身智能体规划与工具使用 |

### 目标模型（Target Models）
- GPT-5.5, GPT-5.4, GPT-5.4-mini, GPT-5.4-nano, Qwen3.6-35B-A3B。

### 对比方法（Baselines）
- **No skill**（无技能基线）
- **Human skill**（人类编写的技能）
- **LLM skill**（由模型一次性生成的技能）
- **Trace2Skill**
- **TextGrad**
- **GEPA**
- **SkillOpt**（主要对比对象）

## 4. 资源与算力

- **论文中未明确提及具体的 GPU 型号、数量或训练时长**。
- 论文重点报告了 **token 消耗量** 作为效率指标：在 GPT-5.5 作为目标模型的全部实验中，Branch2Skill 总计消耗 **1.413 亿（141.3M）token**，而 SkillOpt 消耗 **5.267 亿（526.7M）token**，减少了 73.2%。

## 5. 实验数量与充分性

论文实验设计较为全面，主要包含以下几组实验：

1. **主实验**：涵盖 **5 个目标模型 × 6 个基准 = 30 个设置**，系统对比了 Branch2Skill 与基线方法的性能。
2. **效率对比**：在 GPT-5.5 条件下，对比了 token 消耗和总收益。
3. **模型交互分析**：分别使用 5 种目标模型和 5 种技能模型，共 25 种组合，评估了技能生成模型与执行模型的解耦效果。
4. **消融研究**：对最大深度、子节点数量、迭代预算、证据选择方法（单一路径 / 全树 / Branch2Skill）进行了消融。
5. **迁移实验**：包括跨模型迁移（从 GPT-5.4 迁移到其他模型）和跨数据集迁移（从 OlympiadBench 迁移到 Omni-MATH）。
6. **进化动态分析**：绘制了验证集性能随进化步骤变化的曲线。

**充分性评价**：实验覆盖了多种模型尺寸、多个任务领域，并包含全面的消融与迁移分析，**整体上是充分的**。在公平性方面，论文强调重新运行了 SkillOpt 官方实现并报告了其 token 成本，所有方法遵循相同的任务接口和评分协议，**对比相对客观**。

## 6. 论文的主要结论与发现

1. **性能提升显著**：Branch2Skill 在 30 个设置中排名第一或并列第一的有 26 个，超过 SkillOpt 的有 27 个。在 GPT-5.5 条件下，将平均分从 50.8 提升至 69.1，平均提升 18.3 分（SkillOpt 为 15.3 分）。
2. **效率大幅提升**：相比 SkillOpt，**token 使用量减少 73.2%**，总收益提升 9.3%，每百万 token 的收益提升 4.07 倍（从 0.224 提升至 0.911）。
3. **模型解耦有效**：技能模型与目标模型不必相同，更强的技能模型（GPT-5.5）能带来更好的技能更新效果，尤其是在数学推理任务中。
4. **证据选择至关重要**：对比“单一路径”和“全树”方案，Branch2Skill 的结构化**兄弟节点证据选择效果最佳**，原因在于其局部对比特性。
5. **技能可迁移**：进化得到的技能可以跨模型和跨数据集迁移，说明学到的是可复用的程序性知识而非记忆格式。

## 7. 优点：方法或实验设计上的亮点

- **方法创新性强**：将树搜索的结构优势（共享前缀对比）转化为可操作的学习信号，有效解决了单轨迹反馈稀疏的问题。
- **高效性显著**：一次搜索即可提供多步骤监督，大幅降低 token 成本，实用性较强。
- **通用性良好**：方法在推理（数学、问答）和智能体（ALFWorld、表格操作）等不同类型的任务中都表现良好。
- **模型解耦设计**：允许使用强模型（如 GPT-5.5）作为技能优化器来指导较弱的模型，实验表明这种组合能带来性能提升。
- **验证门控机制**：严格的非回归验证确保了技能的更新不会损害已有能力，增强了方法的稳定性和可靠性。

## 8. 不足与局限

- **对搜索预算的依赖**：方法的性能高度依赖于搜索深度、子节点数和迭代预算，这在实际应用中可能需要根据任务进行调整，且搜索本身仍有计算开销。
- **验证集依赖**：非回归门控依赖于固定的验证集，如果验证集不能代表真实任务分布，可能会限制技能的泛化潜力。
- **技能模型的瓶颈**：在数学推理等复杂任务中，技能更新质量仍受限于技能模型（优化器）的能力，且论文未深入探讨技能模型与目标模型能力差距过大时的表现。
- **长期性与遗忘问题**：论文只进行了**单轮进化**，未深入探索技能在长期持续学习过程中的累积、遗忘或相互冲突问题，也没有评估技能库持续增长后的可维护性。
- **基准时效性与应用范围**：论文使用的基准多为较新的评测集，但实际部署中的开放式任务可能更为复杂，对技能的可解释性和安全性要求更高，论文对此讨论有限。

---

（完）
