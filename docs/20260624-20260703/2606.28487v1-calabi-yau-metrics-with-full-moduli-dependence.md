---
title: Calabi-Yau Metrics with Full Moduli Dependence
title_zh: 具有完整模依赖性的卡拉比-丘度量
authors: "Andrei Constantin, Seung-Joo Lee, Andre Lukas, Luca A. Nutricati"
date: 2026-06-26
pdf: "https://arxiv.org/pdf/2606.28487v1"
tags: ["query:sr"]
score: 9.0
evidence: 结合机器学习的数值数据与符号回归
tldr: 弦论中Calabi-Yau度量的模依赖对现象学至关重要，但解析形式难以获得。本文结合机器学习数值数据和符号回归，提出一种显式Kähler势Ansatz，其系数包含模依赖。通过拟合数值数据并利用符号回归重构解析系数，得到了具有显式复结构和Kähler模依赖的近似Ricci平坦度量。在P^2×P^2中单参数双三次三流形族上，该解析度量与数值数据误差达百分级别，首次实现了完整模依赖的近似解析度量。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.28487v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1555, \"height\": 763, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.28487v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1615, \"height\": 1604, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.28487v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1581, \"height\": 502, \"label\": \"Figure\"}]"
motivation: 现有数值Calabi-Yau度量缺乏解析形式且不含模依赖，限制其在弦论现象学中的应用。
method: 构造带模依赖系数的Kähler势Ansatz，拟合数值数据后应用符号回归解析化系数。
result: 在单参数双三次三流形族上获得与数值数据百分级一致的解析Ricci平坦度量。
conclusion: 首次构建了具有完整模依赖的近似Calabi-Yau度量解析表达式，为现象学研究提供新工具。
---

## 摘要
近期数值方法和机器学习方法的进展使得可以在紧致卡拉比-丘三维流形上高度精确地构造里奇平坦度量。对于现象学应用而言，理解这些度量如何在模空间中变化至关重要。在本工作中，我们通过将机器学习得到的数值数据与符号回归相结合，构建了具有明确复结构和凯勒模依赖性的里奇平坦卡拉比-丘度量的近似解析表达式。我们的方法基于一个包含模依赖性系数的显式凯勒势Ansatz。将该Ansatz拟合到数值数据并应用符号回归，我们能够重建这些系数的解析公式，从而获得具有显式模依赖性的近似里奇平坦度量。我们将该构造应用于$\mathbb{P}^2 \times \mathbb{P}^2$中的单参数双三次三维流形族，达到了与底层数值数据百分之一级别的一致性。

## Abstract
Recent advances in numerical and machine-learning methods have enabled highly accurate constructions of Ricci-flat metrics on compact Calabi-Yau three-folds. For phenomenological applications it is crucial to understand how these metrics vary across moduli space. In this work, we construct approximate analytic expressions for Ricci-flat Calabi-Yau metrics with explicit complex-structure and Kähler moduli dependence by combining machine-learned numerical data with symbolic regression. Our approach is based on an explicit Ansatz for the Kähler potential with moduli-dependent coefficients. Fitting this Ansatz to numerical data and applying symbolic regression allows us to reconstruct analytic formulae for these coefficients, thereby obtaining approximate Ricci-flat metrics with explicit moduli dependence. We apply the construction to a one-parameter family of bi-cubic three-folds in $\mathbb{P}^2 \times \mathbb{P}^2$, achieving percent-level agreement with the underlying numerical data.

---

## 论文详细总结（自动生成）

# 论文总结：具有完整模依赖性的 Calabi-Yau 度量

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究背景**：弦论现象学的一个重要目标是从额外维的几何和模量导出低能有效理论的耦合常数和质量，而不是将其视为输入参数。近年的数值和机器学习方法已能在紧致 Calabi-Yau 三维流形上高精度近似里奇平坦度量，并用于计算物理量（如 Yukawa 耦合、费米子质量）。
- **核心问题**：这些物理观测量随模空间（复结构与 Kähler 模）变化，但现有的数值度量仅在单点处有效，缺乏显式的解析表达式，难以进行模依赖的微分、外推和深层几何分析。
- **整体含义**：首次构建了同时显式依赖复结构模和 Kähler 模的近似解析里奇平坦度量，为弦论现象学中模空间的物理量追踪提供了新工具，精度达百分级别。

## 2. 论文提出的方法论：核心思想、关键技术细节

### 核心思想
通过一个带模依赖系数的解析 Ansatz 近似 Kähler 势，利用神经网络学习数值度量，拟合 Ansatz 系数，再应用符号回归得到系数的显式解析公式，最终获得完整模依赖的近似里奇平坦度量。

### 关键技术细节
1. **Kähler 势 Ansatz**：
   - 形式：\( K(x, \psi, t) = K_{\text{FS}}(x, \psi, t)\big|_X + \phi(x, \psi, t) \)
   - \( K_{\text{FS}} \) 为限制在 CY 流形上的环境空间 Fubini-Study Kähler 势，决定 Kähler 类。
   - 修正项 \( \phi \) 基于线丛 \( L = O_X(k) \) 的全局截面构造：
     \[
     \phi = V(t)^{1/3} \frac{\sum_{I,J} \alpha_{IJ}(\psi, t_1/t_m,\ldots, t_{m-1}/t_m) s_I \bar{s}_J}{\prod_r \left(\sum_a |x_a^{(r)}|^2\right)^{k_r}}
     \]
     其中系数 \( \alpha_{IJ} \) 依赖复结构模 \( \psi \) 和 Kähler 模比 \( t_i/t_m \)。

2. **拟合过程**：
   - 在模空间离散点网格上，使用 `cymetric` 包中的神经网络学习里奇平坦度量。
   - 对每个网格点，通过最小化损失确定 Ansatz 系数 \( \alpha_{IJ} \) 的数值。

3. **符号回归**：
   - 使用 PySR 包对拟合得到的系数数值进行符号回归，得到显式解析表达式（例如含对数、多项式、指数等项的公式）。

4. **对称性简化**：
   - 利用目标 CY 流形（双三次三流形）的大离散对称性（群序 972），将一般 36×36 的系数矩阵简化为 8 个独立的不变量 \( I_i \) 及其系数 \( \alpha_i \)，并满足对称性关系（如 \( \alpha_1(\psi, t_{12}) = \alpha_7(\psi, t_{21}) \) 等）。

## 3. 实验设计

- **目标流形**：\(\mathbb{P}^2 \times \mathbb{P}^2\) 中的单参数双三次三流形族，定义多项式 \( P = \sum_a x_a^3 y_a^3 + \psi \sum_{a\neq b} x_a^3 y_b^3 \)，Hodge 数 (2,83)。
- **模空间采样**：
  - 复结构模 \( \psi = m + in \)，\( m,n = -4,\ldots,5 \)，排除奇异点 \( \psi = 0,\pm1 \)，共 97 个点。
  - Kähler 模比 \( t_1/t_2 \) 取 21 个值（包括 1/11, 1/9, …, 1, …, 11/1 等）。
  - 总计 \( 97 \times 21 = 2037 \) 个模空间点。
- **训练数据**：对每个模空间点，在 CY 流形上随机采样 \( N = 200,000 \) 个点。
- **基准与对比**：
  - 使用神经网络（`cymetric` ϕ-model）作为数值基准，其 σ-loss（Monge-Ampère 方程偏差）在 0.03–0.07 之间。
  - 将解析度量与神经网络度量的 Kähler 势逐点比较平均百分比偏差。
- **无其他方法对比**：本文未对比其他解析度量构造方法（因为目前不存在同类方法）。

## 4. 资源与算力

论文**未明确说明**使用的 GPU 型号、数量、训练时长等具体算力信息。仅提及使用了 `cymetric` 包进行神经网络训练，但未给出硬件细节。符号回归使用 PySR 在 CPU 上运行，但未说明时间消耗。

## 5. 实验数量与充分性

- **实验数量**：覆盖 2037 个模空间点，每点 200,000 个采样点，共约 4 亿个样本点。符号回归后得到 8 个系数的完整解析表达式。
- **充分性评估**：
  - 优点：采样密度较高，覆盖了复结构和 Kähler 模的宽范围，并避免了奇异点。
  - 不足：
    1. 仅测试了一个单参数族（仅一个复结构模和两个 Kähler 模），未能验证更高维模空间或更复杂流形。
    2. 缺乏消融实验——未研究不同线丛度 \( k \) 对精度的影响（仅用了 \( k=(2,2) \)）。
    3. 未与纯符号回归（直接最小化 Ricci 平坦性损失）对比性能。
  - 客观性：数值数据使用成熟工具 `cymetric`，符号回归使用标准包 PySR，方法透明。

## 6. 论文的主要结论与发现

1. **成功构造**：得到了第一个同时显式依赖复结构和 Kähler 模的近似解析里奇平坦度量。
2. **精度**：解析度量的平均 σ-loss 约 0.06，Kähler 势平均百分比偏差约 3.2%，已达百分级别。
3. **低度线丛足够**：使用 \( k=(2,2) \) 的线丛（36 个截面）即可得到良好近似，表明几何核心特征已被捕获。
4. **奇异点表现**：符号回归在 \( \psi=0 \) 处出现人工奇点，但 \( \psi=1 \) 处正常；尚不清楚是否为数值伪影。
5. **对称性利用**：离散对称性大幅简化了问题，使 Ansatz 参数从 1296 个减至 8 个。

## 7. 优点：方法或实验设计上的亮点

- **统一模依赖**：首次同时处理复结构和 Kähler 模的显式依赖，而之前工作分别处理其中之一。
- **混合方法**：结合神经网络的学习能力和符号回归的可解释性，获得解析表达式。
- **对称性降低复杂度**：巧妙利用大离散对称性将待拟合参数从 1296 锐减至 8 个，使符号回归可行。
- **开源工具链**：使用 `cymetric`、PySR 等可复现工具，方法具有推广性。
- **精度可接受**：百分级误差对弦论现象学计算（如 Yukawa 耦合）已有实用价值（先前仅用 FS 度量误差约 20%）。

## 8. 不足与局限

- **应用范围有限**：仅测试了一个高度对称的单参数（h²¹=1）双三次族，未推广到更一般 CY 流形（如更多模、更少对称性）。
- **奇异点处理模糊**：ψ=0 处符号回归出现人工奇点，尚未明确该奇点是否本质或伪影，需更密集采样验证。
- **线丛度未优化**：未系统研究增加 k 是否会收敛到真正 Ricci 平坦解，当前精度可能受限于低度。
- **无消融实验**：未比较不同神经网络架构、采样点数、符号回归设定对结果的影响。
- **计算资源未公开**：难以评估方法的可负担性和可复现性。
- **未直接计算物理量**：虽然声称百分级度量可用于现象学，但未实际计算 Yukawa 耦合等观测量并给出误差界。

（完）
