---
title: Searching the Space of Feed-Forward Neural-Network Weight-Update Rules with Fixed Depth Symbolic Regression
title_zh: 使用固定深度符号回归搜索前馈神经网络权重更新规则空间
authors: "Charles Brum, Edward Finkelstein"
date: 2026-07-23
pdf: "https://arxiv.org/pdf/2607.21855v1"
tags: ["query:sr"]
score: 9.0
evidence: 用符号回归发现神经网络权重更新规则
tldr: "标准优化器可能并非最优，本文用固定深度符号回归搜索前馈神经网络的权重更新规则。在30个基准/网络组合中，25个案例发现了优于最佳超参数调优优化器的规则，平均MSE降低44.47%。发现规则形式多样，融合了自适应归一化、动量等。结果表明符号回归可轻量发现更优优化规则，但需更大规模验证。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.21855v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1005, \"height\": 1587, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.21855v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1249, \"height\": 543, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.21855v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1250, \"height\": 1591, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.21855v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1547, \"height\": 949, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.21855v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1461, \"height\": 1050, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.21855v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1360, \"height\": 350, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.21855v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1357, \"height\": 376, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.21855v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 780, \"height\": 317, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.21855v1/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1445, \"height\": 989, \"label\": \"Table\"}]"
motivation: 探索符号回归能否发现比标准优化器更优的神经网络权重更新规则。
method: 将候选规则表示为固定深度符号表达式，基于梯度、动量、自适应梯度等操作数进行搜索。
result: "在30个基准/网络组合中，25个案例找到优于最佳调优优化器的规则，平均MSE降低44.47%。"
conclusion: 符号回归可轻量发现紧凑优化器变体，但需更大规模验证。
---

## 摘要
我们研究符号回归是否能够发现显式的神经网络权重更新规则，这些规则在小型符号回归基准上优于标准的手工设计优化器。候选更新规则表示为从常见优化器（包括梯度、动量、自适应梯度和矩估计量）派生的操作数上的固定深度符号表达式。在30个基准/神经网络组合中，符号回归过程发现了25个案例中优于最佳超参数调优的现有优化器的更新规则，在改进的案例中总均方误差降低了44.47%。发现的规则并不共享单一的通用符号形式，但许多规则结合了自适应归一化、动量类量、非线性变换和有理表达式。这些结果表明，符号回归可以作为发现紧凑优化器变体的轻量级机制，同时也突出了对更大规模验证的需求。

## Abstract
We investigate whether symbolic regression can discover explicit neural network weight-update rules that outperform standard hand-designed optimizers on small symbolic regression benchmarks. Candidate update rules are represented as fixed-depth symbolic expressions over operands derived from common optimizers, including gradient, momentum, adaptive-gradient, and moment-estimate quantities. Across 30 benchmark/neural network combinations, the symbolic regression procedure found an update rule outperforming the best hyperparameter-tuned established optimizer in 25 cases, with an aggregate MSE reduction of 44.47\% over the improved cases. The discovered rules do not all share a single common symbolic form, but many combine adaptive normalization, momentum-like quantities, nonlinear transformations, and rational expressions. These results suggest that symbolic regression can serve as a lightweight mechanism for discovering compact optimizer variants, while also highlighting the need for larger-scale validation.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：标准手工设计的神经网络优化器（如 SGD、Adam 等）通常基于启发式原则，是否可以通过符号回归（Symbolic Regression）自动发现更优的显式权重更新规则？
- **背景**：已有大量工作尝试学习优化算法（如 meta-learning、LSTM 优化器、AutoML-Zero 等），但大多数方法要么需要大量计算，要么得到的更新规则不够紧凑/可解释。本文尝试用固定深度（fixed depth）的符号回归，在受限的表达式空间中搜索简洁的更新规则，并在小型符号回归基准上验证其相对于超参数调优的标准优化器的性能优势。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：将神经网络权重更新规则表示为固定深度的符号表达式（表达式树），操作数来源于标准优化器中出现的量（如梯度、动量、一阶/二阶矩估计等），通过遗传编程（Genetic Programming）演化出使训练后网络损失最小的更新规则。
- **技术细节**：
  - **表示**：每个更新规则是深度固定为 5 的表达式树，操作数包括：当前权重 `w`、梯度 `g`、速度 `v`、一阶矩 `μ`、二阶矩 `ν`、累加梯度平方和 `σg²`、指数移动平均 `E[g²]`、`E[∆w²]`、Adam 偏置修正项 `μ̂`、`ν̂` 以及若干超参数（`η`, `θ`, `γ`, `ϵ`, `β1`, `β2`）。
  - **算子集合**：一元算子（`-`, `ln`, `exp`, `cos`, `sin`, `sqrt`, `asin`, `acos`, `tanh`），二元算子（`+`, `-`, `*`, `/`, `^`）。
  - **搜索算法**：基于多线程的遗传编程，每个线程独立运行，种群大小最大为 100 个候选规则。每个候选规则用其训练一个随机初始化神经网络 10 个 epoch 后的均方误差（MSE）作为适应度。
  - **进化策略**：尝试用之前基准中表现最好的规则作为后续基准的初始种群种子（seeding）；若进化停滞则回退到随机初始化。
  - **终止条件**：当连续多代适应度无显著改善，或已找到超过 100 个优于基线优化器的规则时停止。

## 3. 实验设计：数据集/场景、基准、对比方法

- **数据集/场景**：10 个符号回归基准函数，其中 5 个来自 Hemberg 等（2008），5 个来自 AI Feynman（Udrescu & Tegmark, 2020）。每个基准函数是一个由公式定义的回归任务，输入维度 2~8 不等，输出为标量。
- **基准（baseline）**：8 种标准优化器：Gradient Descent、Heavy-Ball Momentum、Nesterov Accelerated Gradient（NAG）、AdaGrad、RMSProp、AdaDelta、Adam、AdamW。对每种优化器进行超参数网格搜索（表 3），得到各自最优超参数组合下的 MSE。
- **对比方法**：符号回归发现的规则 vs. 基准优化器的最优超参数版本。
- **神经网络架构**：3 种不同的前馈网络（NN1, NN2, NN3），均使用 sigmoid 激活和线性激活（无激活）的组合，深度为 2~3 层，宽度不等（见文中图 1）。输入节点数由基准函数输入维度决定。
- **总实验数**：10 个基准 × 3 个网络架构 = 30 个组合。

## 4. 资源与算力

- **文中未明确说明 GPU 型号、数量、训练时长**。仅提到实现为多线程 C++ 代码，种群规模最多 100 个候选规则，每个规则只训练 10 个 epoch（训练更长时间会让计算代价过高）。作者将计算成本限制在“轻量级”范围内，未报告具体运行时间或硬件信息。

## 5. 实验数量与充分性

- **数量**：30 组实验，每组对应一个基准-网络组合。在每组中，符号回归会演化出若干候选规则，报告找到的最佳规则及其 MSE，并记录有多少个规则优于最优基线（从 0 到 100）。
- **充分性与公平性**：
  - 优化器超参数调优：网格搜索范围较广，但文中未明确说明是否对每个优化器都调优了所有超参数（例如只调了学习率、动量系数、衰减率等），且调优结果依赖于随机种子（文中未提及使用多随机种子）。这可能导致基线结果存在偏差。
  - 符号回归实验：每次只运行一次演化（未报告多次随机种子平均结果），且终止条件可能不统一（未明确收敛标准）。
  - 网络规模小、训练轮次短（10 epoch），与大规模深度学习场景差距较大。
  - 对比仅基于 MSE，未考虑其他指标（如收敛速度、泛化能力、计算开销等）。
  - 总体而言，实验覆盖度有限，缺乏统计显著性检验和泛化验证，公平性受限于调优和随机性控制不足。

## 6. 论文的主要结论与发现

- 符号回归在 30 个组合中的 25 个找到了优于最佳超参数调优基线优化器的更新规则，改进案例中平均 MSE 降低 44.47%。
- 发现的规则**不统一**，但出现了一些重复出现的“签名”结构（如涉及累积梯度统计、动量项、有理表达式等）。
- 许多规则组合了自适应归一化、动量、非线性变换等要素，与常见优化器设计有相似之处。
- 失败案例主要集中在最大的网络架构（NN3）上，暗示随网络复杂度增加，自动发现的规则可能不再有利。

## 7. 优点：方法或实验设计上的亮点

- **创新性**：将符号回归应用于优化器自动发现，并使用固定深度限制保持规则简洁可解释；操作数直接来自主流优化器，搜索空间有针对性。
- **轻量级**：与传统 meta-learning 不同，只训练 10 epoch 即可评估候选规则，计算开销较低。
- **可解释性**：发现的显式规则可以检查和简化，有助于理解优化机制。
- **种子策略**：尝试跨基准迁移已发现规则作为初始种群，探索优化器泛化性。
- **结果积极**：在大多数小规模测试上优于手工优化器，展示该方向的潜力。

## 8. 不足与局限

- **实验规模小**：神经网络只有 3 种小型结构，训练仅 10 个 epoch，未在大型模型或标准数据集（如 CIFAR、ImageNet、语言模型）上验证，泛化性存疑。
- **缺乏统计稳健性**：每个实验仅运行一次，未报告多次随机种子下均值和方差，无法评估性能的可靠性。
- **基线调优可能不充分**：超参数网格搜索范围有限，可能未找到真正最优基线；且不同优化器的调优粒度不一致。
- **搜索策略局限**：固定深度 5 限制了表达复杂度；种群大小小、演化代数未明确，可能无法充分探索空间。
- **终止条件模糊**：“直到改进可忽略”描述不精确，可能过早停止。
- **公平性疑虑**：符号回归的适应度评估本身使用相同训练数据，可能导致过拟合于特定基准/网络/epoch 数，而基线优化器可能更需长训练才能发挥优势。
- **计算资源未报告**：无法评估方法的具体成本或可扩展性。
- **未讨论数值稳定性**：符号规则可能产生 NaN 或无穷大，文中仅提到“early termination if weights become undefined”，但未分析稳定性如何影响结果。

（完）
