---
title: Learning Parametric Nitrogen Fertilizer Response Curves Using Neuro Symbolic Regression
title_zh: 利用神经符号回归学习参数化氮肥响应曲线
authors: "Giorgio Morales, John Sheppard"
date: 2026-05-29
pdf: "https://arxiv.org/pdf/2605.31276v1"
tags: ["query:sr-piml"]
score: 9.0
evidence: 神经符号回归方法学习氮肥响应曲线
tldr: 精准农业中建模作物对氮肥的响应面临挑战，现有方法依赖预定义参数形式或黑箱模型，难以解释或发现特定地点的函数关系。本文提出神经符号回归方法，利用Transformer多集符号骨架预测策略，在不预设函数形式的情况下学习参数化氮响应曲线，并通过遗传算法拟合观测数据。在合成数据和真实冬小麦数据上验证，该方法在数据稀缺时仍能恢复正确表达式，且发现的曲线比传统二次平台和指数函数拟合误差更低，能捕获不同管理区的功能行为。展示了神经符号回归在发现地点特异性农学关系和支持精准农业决策中的潜力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.31276v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 707, \"height\": 393}, {\"url\": \"assets/figures/arxiv/2605.31276v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 842, \"height\": 267}, {\"url\": \"assets/figures/arxiv/2605.31276v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 838, \"height\": 959}, {\"url\": \"assets/figures/arxiv/2605.31276v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 854, \"height\": 415}, {\"url\": \"assets/figures/arxiv/2605.31276v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 923, \"height\": 697}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.31276v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 930, \"height\": 650}, {\"url\": \"assets/tables/arxiv/2605.31276v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 916, \"height\": 497}, {\"url\": \"assets/tables/arxiv/2605.31276v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 776, \"height\": 385}, {\"url\": \"assets/tables/arxiv/2605.31276v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 660, \"height\": 329}]"
motivation: 现有氮肥响应模型依赖固定函数形式或黑箱模型，缺乏灵活性和可解释性，无法自动发现数据中潜在的函数关系。
method: 提出神经符号回归方法，结合Transformer多集符号骨架预测策略，从多个子域数据中学习共享符号结构，再用遗传算法拟合参数。
result: 在合成数据上即使数据稀缺也能恢复正确表达式；在真实冬小麦数据上，发现的管理区特异性氮响应曲线拟合误差低于传统函数模型。
conclusion: 该方法能自动发现可解释的、地点特异性的氮肥响应函数，为精准农业提供数据驱动的决策支持。
---

## 摘要
准确模拟作物对氮肥施用的响应是精准农业中的一个基本挑战，因为它既影响经济效益也影响环境可持续性。现有方法要么依赖预定义的参数化形式，要么使用不透明的机器学习模型，这限制了它们从数据中解释或发现特定地点的功能关系的能力。在这项工作中，我们提出了一种神经符号回归方法，可以在不假设预定义函数形式的情况下学习参数化的氮响应曲线。我们的方法结合了基于Transformer的多集符号骨架预测策略，能够发现跨多个子域或管理区的共享功能结构。通过构建多样化的输入子集并强制它们之间的一致性，该方法恢复了稳健的符号骨架，随后使用遗传算法将其拟合到观测数据上。该框架首先在一维合成问题上进行评估，以测试其在不同程度认知不确定性下的鲁棒性。结果表明，即使在数据稀缺的情况下，所提出的符号回归方法也能恢复正确的表达式。在本文中，我们展示了将该方法应用于真实冬小麦数据的结果，为田块内的不同管理区学习了不同的参数化氮响应曲线。结果表明，发现的表达式不仅比传统模型（如二次平台和指数函数）具有更低的拟合误差，而且还捕获了跨空间区域的多样化功能行为。这展示了神经符号回归在发现特定地点的农学关系以及支持精准农业中知情决策方面的潜力。

## Abstract
Accurately modeling crop response to Nitrogen (N) fertilization is a fundamental challenge in precision agriculture, as it impacts both economic returns and environmental sustainability. Existing approaches either rely on predefined parametric forms or opaque machine learning models, limiting their ability to interpret or discover site-specific functional relationships from data. In this work, we propose a neuro symbolic regression (SR) approach to learn parametric N-response curves without assuming a predefined functional form. Our approach integrates a transformer-based Multi-Set Symbolic Skeleton Prediction strategy, enabling the discovery of shared functional structures across multiple subdomains or management zones (MZs). By constructing diverse input subsets and enforcing consistency across them, the method recovers robust symbolic skeletons that are subsequently fitted to observed data using a genetic algorithm. This framework was first evaluated on synthetic one-dimensional problems to assess its robustness under varying levels of epistemic uncertainty. The results demonstrate the ability of the proposed SR approach to recover correct expressions even in data-scarce regimes. In this work, we present the results of applying our method to real-world winter wheat data, learning distinct parametric N-response curves for different MZs within a field. The results show that the discovered expressions not only achieve lower fitting errors than traditional models such as quadratic-plateau and exponential functions, but also capture diverse functional behaviors across spatial regions. This demonstrates the potential that neuro SR has to enable the discovery of site-specific agronomic relationships and support informed decision-making in precision agriculture.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

精准农业中，准确建模作物产量对氮肥施用的响应（N-response）至关重要，直接影响经济收益与环境保护。传统方法要么依赖预定义的参数化函数（如二次-平台、指数函数），要么使用黑箱机器学习模型（如神经网络），前者缺乏灵活性，后者难以解释和发现场地特异性的功能关系。该论文旨在解决“如何自动、可解释地从数据中学习氮肥响应曲线的数学形式”这一根本问题，提出一种神经符号回归方法，在不预设函数形态的前提下，从神经网络生成的响应曲线中提取简洁的数学表达式，为精准农业中的决策支持提供透明、可解释的工具。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程（用文字说明）

**核心思想**：将管理区（MZ）内部的氮肥响应曲线建模为一维函数，利用“多集符号骨架预测”策略，从多个随机采样的输入子集中恢复出共享的符号骨架（即仅含占位符的函数形式），然后通过遗传算法拟合具体参数。

**关键技术细节**：
- **神经符号回归框架（SeTGAP变体）**：原本SeTGAP用于多元符号回归，这里将其适配到一维问题。
- **多集符号骨架预测（MSSP）**：对于同一曲线，随机采样多个不同子区间（覆盖80%~100%的输入域），构成多个输入-输出集合。由于这些集合来自同一个函数，它们共享相同的符号骨架。使用预训练的Transformer模型（Multi-Set Transformer）从这些集合中预测共享骨架。
- **算法流程（Algorithm 1）**：
  1. 给定观测数据集 \(D\) 和模型预测的网格数据 \(\hat{D}\)。
  2. 重复 \(n_{\text{cand}}\) 次：
     - 对于每次，构造 \(N_S\) 个输入集：每个集通过从输入域中随机选择一个子范围，并从 \(\hat{D}\) 中选取并插值出 \(n\) 个点得到。
     - 使用Multi-Set Transformer \(g\) 生成 \(n_B\) 个候选骨架。
  3. 去重后得到候选骨架列表。
  4. 对每个候选骨架，使用遗传算法（GA）优化其系数，最小化与观测数据 \(D\) 的MSE。
  5. 返回MSE最小的表达式 \(\tilde{f}(x)\) 及其骨架 \(\tilde{e}(x)\)。
- **对于管理区内多个曲线（Algorithm 2）**：从同一管理区内随机选取 \(N_S\) 条不同的N响应曲线，从每条曲线中采样一个子集，构成 \(N_S\) 个输入集（每个来自不同曲线但共享相同的MZ功能形式），进而预测骨架。最终骨架通过在一组随机保留的测试曲线上拟合后选择MSE最小的。

**公式示意**：最终得到的表达式形式例如 \(c_1 + c_2 \tanh(c_3 + c_4 x_{\text{Nr}})\) 或 \(c_1 + c_2 \exp(c_3 \sin(c_4 + c_5 x_{\text{Nr}}))\)，其中系数由GA拟合。

### 3. 实验设计：使用了哪些数据集 / 场景，它的 benchmark 是什么，对比了哪些方法

- **合成数据实验**：
  - 使用三个含异方差噪声的一维函数：`cos`（\(10 - 5\sin(0.2x^2+1.6)\)）、`hetero`（\(7\sin(x - 18.8)\) 但非标准）、`cosqr`（\(0.2\sin(x^2)\) 相关）。初始数据稀疏，导致高认知不确定性。
  - 在自适应采样（ASPINN）框架下，每次迭代增加5个新样本，共50次迭代。
  - 对比目标：恢复的表达式是否与真实函数形式一致，并报告MSE。
  - **基准（benchmark）**：无其他符号回归方法对比，主要是检验自身方法在不确定性降低过程中的收敛性。

- **真实农业数据**：
  - 冬小麦田块“Field A”，预先使用2D-3D卷积神经网络训练了一个早期产量预测模型，并为每个田地点生成N-response曲线（N率0~150 lb/acre）。
  - 基于先前的管理区（MZ）划分（通过功能主成分分析聚类得出4个MZ），每个MZ内的曲线形态相似。
  - 对比方法：传统二次-平台模型和指数模型，以及本文SeTGAP发现的骨架（每个MZ一个）。
  - 评估指标：每个MZ内拟合所有站点的平均绝对误差 \(\bar{r}\)。

### 4. 资源与算力：如果文中有提到，请总结使用了多少算力（GPU 型号、数量、训练时长等）。若未明确说明，也请指出这一点。

文中**未明确提及**使用的GPU型号、数量、训练时长等算力信息。仅提及使用了预训练的Multi-Set Transformer模型（来自[11]），未详述其训练成本。在合成数据和真实数据上的实验均在一台普通计算设备上运行，但未提供具体配置。

### 5. 实验数量与充分性：大概做了多少组实验（如不同数据集、消融实验等），这些实验是否充分、是否客观、公平

- **合成数据**：3个函数（cos, hetero, cosqr），每个函数在自适应采样下进行了50次迭代实验（每次迭代运行一次符号回归并记录表达式）。展示了关键迭代（1,5,10,...,50）的结果。未做消融（如不同NS、n、ncand的敏感性分析），但提到增加这些值无额外收益。
- **真实数据**：4个MZ，每个MZ独立进行骨架生成和拟合，对比3种方法（SeTGAP、二次-平台、指数）。未做交叉验证或在不同田块上验证。
- **充分性判断**：合成实验充分验证了方法在不确定性下的收敛性；真实实验仅针对单一天气田块，作为概念验证。缺乏与多种符号回归方法（如DSR、Eureqa等）的横向比较，也未在多农场、多作物上验证泛化性。实验设计总体合理，但覆盖范围有限。

### 6. 论文的主要结论与发现

- 在合成数据上，提出的多集符号回归策略能够在数据稀疏、预测不确定性高时逐渐恢复正确的函数形式，即使早期预测模型有偏差，多数子集仍支持正确骨架。
- 在真实冬小麦数据上，发现的每个MZ的符号表达式拟合误差均低于传统二次-平台和指数模型，且能捕获不同MZ的不同功能行为（例如MZ1和MZ2采用tanh形式，MZ3采用线性加余弦，MZ4采用指数嵌套正弦）。
- 这些可解释的方程可作为替代昂贵神经网络的轻量级模型，支持高效的经济最优施氮量（EONR）计算和多目标优化。

### 7. 优点：方法或实验设计上有哪些亮点

- **创新性**：首次将神经符号回归应用于氮肥响应曲线的自动发现，无需预设函数形式。
- **鲁棒性**：通过多集采样设计，即使局部高不确定性区域存在，也能从多个子集中恢复共享骨架，抵抗过拟合和噪声。
- **可解释性**：输出的是数学表达式，农学家可直接理解函数形态（如饱和、线性、周期性等），优于黑箱模型。
- **应用价值**：生成的表达式可直接用于经济最优施氮量计算，且计算效率高，便于未来多目标优化集成。
- **迁移性**：方法可应用于其他一维响应曲线学习问题（如灌溉、播种率）。

### 8. 不足与局限：包括实验覆盖、偏差风险、应用限制等

- **实验覆盖有限**：仅在单个冬小麦田块的4个管理区上验证，缺乏多田块、多作物（玉米、大豆等）和不同气候条件下的泛化测试，存在过拟合到特定数据集的风险。
- **方法比较不充分**：未与主流的符号回归方法（如基于遗传编程的Eureqa、深度符号回归DSR、基于梯度的SymbolicGPT等）进行对比，无法评估本文方法相对于现有方法的技术优势。
- **方法论特设**：当前策略是为1D问题专门设计的，缺乏对更高维SR问题的扩展和形式化，作者也承认这是“ad hoc solution”。
- **依赖预训练Transformer**：Multi-Set Transformer需要在大规模符号骨架语料上预训练，训练成本高，且模型的可迁移性未验证。
- **假设管理区内函数形式一致**：这一依赖聚类质量的假设若聚类不准确，可能导致骨架不适合某些站点。
- **未考虑实际农学约束**：例如曲线应单调递增且饱和，但发现的表达式（如包含余弦项）可能在物理上不可解释，需要后续人工审查。

（完）
