---
title: A Data-Free Symbolic Regression Approach for Solving Equations
title_zh: 一种无数据符号回归方程求解方法
authors: "Sergei Garmaev, Vinay Sharma, Olga Fink"
date: 2026-06-05
pdf: "https://arxiv.org/pdf/2606.07152v1"
tags: ["query:sr-piml"]
score: 9.0
evidence: 无需数据的符号回归求解方程
tldr: 许多科学方程无法解析求解，只能依赖数值解。现有符号回归方法需要训练数据，而本文提出符号方程求解器SES，将方程求解转化为可微符号模型的优化问题，仅利用方程本身及初始或边界条件构造目标函数，无需任何数据。在代数方程、常微分方程和偏微分方程上的实验表明，SES能够恢复与解析解匹配的紧凑符号表达式，为方程求解提供了无需数据的新途径。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.07152v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1553, \"height\": 1287, \"label\": \"Figure\"}]"
motivation: 现有符号回归方法需要数据，而许多方程只有方程本身，缺乏数据驱动的方法。
method: 将方程求解表示为可微符号模型的优化，目标函数由方程残差和边界条件构成。
result: 在代数、常微分和偏微分方程上，SES成功恢复出与解析解一致的紧凑符号表达式。
conclusion: SES实现了无需数据的符号回归求解方程，输出的显式符号表达式便于后续分析。
---

## 摘要
当前科学中出现的许多方程无法通过现有的解析技术求解，因此只能进行数值求解，而不能得到显式的符号表达式。现有的符号回归方法可以恢复符号表达式，但需要从底层过程中获取训练数据，而非仅仅依赖控制方程。我们提出了符号方程求解器（SES），这是一个将方程求解转化为可微符号模型上优化问题的框架。SES从方程以及初始条件或边界条件构建目标函数，从而消除了对配对的输入输出数据的需求。学习到的模型以显式符号形式表达，便于进一步分析。我们在具有代表性的代数方程和微分方程上评估了SES，包括一个代数方程组、一个含有超越项的方程、一个常微分方程以及具有不同初始条件或边界条件的偏微分方程。在这些设定下，SES恢复了与相应解析解匹配的紧凑符号表达式。

## Abstract
Many equations arising in science currently cannot be solved by available analytical techniques and are therefore solved numerically, without yielding explicit symbolic expressions. Existing symbolic regression approaches can recover symbolic expressions, but require training data obtained from the underlying process, rather than the governing equation alone. We propose the Symbolic Equation Solver (SES), a framework that formulates equation solving as an optimization problem over differentiable symbolic models. SES constructs its objective from the equation together with initial or boundary conditions, eliminating the need for paired input-output data. The learned model is expressed in explicit symbolic form, enabling further analysis. We evaluate SES on representative algebraic and differential equations, including a system of algebraic equations, an equation with transcendental terms, an ordinary differential equation, and partial differential equations with different initial or boundary conditions. Across these settings, SES recovers compact symbolic expressions that match the corresponding analytical solutions.

---

## 论文详细总结（自动生成）

### 论文详细中文总结

#### 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：许多科学方程（如含超越项的非线性方程、微分方程）无法通过传统解析方法得到显式闭式解，不得不依赖数值近似。数值解虽精确，但缺乏可解释的符号结构，难以揭示函数依赖关系、对称性和定性行为。现有符号回归方法（如EQL、PySR）可以恢复符号表达式，但**需要配对的输入-输出数据**（即监督学习），而许多实际场景只给出方程本身和初始/边界条件，没有预先计算的解数据。另一方面，残差驱动型神经网络求解器（如PINNs）可直接利用方程残差训练，但输出是神经网络参数化的数值近似，而非显式符号表达式。
- **研究动机与意义**：论文旨在填补一个空白：同时满足三个理想特性——(i) 直接从控制方程和辅助条件出发，(ii) 不需要监督输入-输出数据，(iii) 以显式符号形式恢复解。这有助于在科学计算、降阶建模、控制以及AI辅助数学发现中提供可解释的紧凑符号解。

#### 2. 论文提出的方法论：核心思想、关键技术细节
- **核心思想**：将方程求解转化为**可微符号模型上的优化问题**。用可微符号网络表示候选解 \( \hat{u}(x;\theta) \)（基于EQL架构），构建损失函数为**方程残差**和**初始/边界条件残差**的平方和，在配点处评估，通过梯度下降优化网络参数\(\theta\)，最终从训练后的稀疏网络中提取显式符号表达式。
- **关键技术细节**：
  - **符号模型架构**：采用EQL式全连接网络，激活函数替换为符号运算符库：`{id, const, (·)², exp(·), tanh(·), ×}`。每层最大深度为2，每层每个运算符实例化两次。
  - **损失函数**：
    \[
    \mathcal{L}(\theta) = \frac{1}{N_F}\sum_{i=1}^{N_F}\sum_{k=1}^K \left[F_k(x_i,\hat{u},\nabla\hat{u},\dots)\right]^2 + \frac{1}{N_B}\sum_{i=1}^{N_B}\sum_{j=1}^J \left[B_j(z_i,\hat{u},\dots)\right]^2
    \]
    第一项为控制方程残差，第二项为辅助条件残差。导数通过自动微分计算。
  - **优化策略**：三阶段训练。第一阶段仅用残差损失训练50,000 epochs；第二阶段加入\(\ell_1\)惩罚并周期性剪枝（每次剪掉对损失贡献最小的25%权重），持续200,000 epochs，直到最少活跃权重数（通常设为5）；第三阶段移除惩罚，微调20,000 epochs。最终权重四舍五入到小数点后5位提取表达式。
  - **配点采样**：每个epoch独立采样256个配点（从域内均匀抽样）。对非微分方程，使用辅助变量t（均匀分布）作为输入。

#### 3. 实验设计：数据集/场景、基准、对比方法
- **实验场景（6个方程）**：
  1. 线性方程组：\(2x+3y=7, x-y=1\) → 解 \(x=2, y=1\)
  2. 超越方程：\(e^x + x^3 = e+1\) → 解 \(x=1\)
  3. 非线性ODE：\(dy/dt = 1-y^2, y(0)=0\) → 解 \(y=\tanh t\)
  4. 线性输运PDE：\(\partial_t u + \partial_x u = 0, u(x,0)=x^2\) → 解 \(u=(x-t)^2\)
  5. 二维Poisson方程（第一组边值）：\(\nabla^2 u = 4+2x\)，混合边值 → 解 \(u=x^2+y^2+xy^2\)
  6. 同一Poisson方程（第二组边值） → 解 \(u=2x^2+xy^2\)
- **基准与对比方法**：论文**未进行定量对比实验**。仅定性展示了SES恢复的表达式与解析解一致。没有与PINNs、EQL（有数据版本）、数值求解器等基准方法进行对比。
- **数据使用**：所有实验**不依赖任何监督数据**，仅使用方程和边界条件。

#### 4. 资源与算力
- **明确说明**：论文**未提及**使用的GPU型号、数量、训练时长、内存等任何计算资源信息。仅在方法部分提到优化使用Adam优化器，训练总epochs为270,000（50k+200k+20k）。无具体耗时。

#### 5. 实验数量与充分性
- **实验数量**：共6个方程例，覆盖代数（含系统、超越）、ODE、PDE（含不同边值条件）。每个方程只做一次训练/提取，无重复实验、消融实验或超参数敏感性分析。
- **充分性与客观性评价**：
  - **优点**：多样性强，从简单到复杂递进；所有方程都有已知解析解，便于验证。
  - **不足**：
    - 缺乏定量误差指标（如L2相对误差、残差大小）。
    - 未消融关键设计（如剪枝阈值、\(\ell_1\)系数、层数、运算符集）。
    - 未与任何现有方法（PINNs、数值求解器、有数据符号回归）进行性能对比。
    - 实验规模较小，仅6个案例，不足以证明方法在更复杂问题上（如高维、混沌、奇异）的泛化能力。
    - 未讨论对噪声、随机种子、网络初始化敏感性的影响。

#### 6. 论文的主要结论与发现
- SES能成功**从方程和边界条件出发，无需任何监督数据**，恢复出与解析解匹配的紧凑符号表达式（对线性方程组、超越方程、ODE、PDE均有效）。
- 框架可自然扩展到**多未知量系统**（如代数方程组）和**混合边界条件**。
- 提取的符号表达式保函正确的非线性函数形式（如\(\tanh\)、平方），而非仅仅数值拟合。
- 结论认为SES填补了数据驱动符号回归与方程驱动数值求解器之间的空白，为可解释科学计算提供了新途径。

#### 7. 优点：方法或实验设计上的亮点
- **方法亮点**：
  - **无数据需求**：直接利用方程残差作为监督信号，避免了传统符号回归对输入-输出数据的要求，对仅有方程的场景极为有用。
  - **显式符号输出**：与PINNs等数值近似方法不同，SES输出可读、可进一步分析的闭式表达式。
  - **一体化优化**：将方程求解、约束满足、稀疏化与显式提取统一在可微框架中，可通过自动微分计算导数。
  - **统一处理代数与微分方程**：使用相同框架（仅改变输入变量性质），无需专门设计。
- **实验亮点**：选择了包含线性、非线性、超越、偏微分等多个典型类别的方程，覆盖了不同复杂度的基础案例，清晰展示了可行性。

#### 8. 不足与局限
- **实验覆盖不足**：
  - 仅测试了已知解析解且符号结构简单的方程，未涉及无解析解、混沌、高维、刚性、耦合大规模系统。
  - 运算符库固定且有限（仅6个算子），无法表达三角函数、对数、根号等常见函数（除非通过组合近似），限制了适用范围。
  - 未评估优化成功率（随机种子多次运行）、对初始化的敏感性。
- **偏差风险**：所有例子解都恰好是运算符库集合的直接组合，可能夸大了方法能力。如果真实解不在库中，SES可能只能给出近似或失败。
- **应用限制**：
  - 剪枝策略（固定每25%且最小权重数5）可能过于启发式，对不同方程的适应性未验证。
  - 训练epochs固定且较大（270k），对更复杂方程可能收敛慢或需要更多计算量，但论文未提供计算成本分析。
  - 提取的表达式要求权重准确到小数点后5位，若解含有无理常数（如\(\pi\)、\(\sqrt{2}\)）则无法精确恢复。
- **比较缺失**：未与PINNs、传统数值解法、有数据符号回归进行误差、速度、可解释性的定量比较，无法证明SES在实际应用中的优势。

（完）
